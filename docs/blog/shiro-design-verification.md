# Shiro 暖色设计与数据库验证

日期：2026-09-05。

> 历史视觉记录；当前布局已由 [余白紧凑列表验证](./yohaku-compact-verification.md) 替代，数据库接入记录仍有效。

## 当前方向

按照用户最后确认的方向，博客使用奶白与暖色，文章列表参考
[Shiro 的 PostItem](https://github.com/Innei/Shiro/blob/main/apps/web/src/components/modules/post/PostItem.tsx)
宽松模式：大标题、自然摘要、小缩略图与底部元信息。
结合其轻量悬停反馈独立实现，未引入 Shiro 源码、依赖或运行时。

此前 Dawn 源码仅用于恢复宽屏构图；青色品牌与浅绿色标签已被暖色体系替代。
Yohaku 的窄栏视觉方案属于历史记录，链接预览和移动阅读功能继续保留。

- PC：最大 1240px 站点容器，列表侧栏 264px、文章区域 928px；阅读纸面 820px、目录栏 224px。
- 平板：隐藏 PC 侧栏，主题筛选移到列表顶部；首页保留左右介绍构图。
- 手机：两行页头导航、横向主题筛选、完整标题与小缩略图、独立底部阅读工具栏。
- 列表去掉封闭卡片，使用细分隔线；悬停和键盘聚焦只改变衬底，不改变尺寸。
- 主题色统一维护在 `nubbi-blog/src/styles/theme.css`；没有组件硬编码颜色。

## 数据库与本地服务

- 用户选择 Nubbi 后端直连 `dawnpoi.site` 上的 **Nubbi** 库。
- 只修改本机忽略文件 `server/.env` 中的 `MONGO_DB_NAME`，复用现有 URI 和凭据。
- 后端已重启并连接成功，`/blog/posts`、`/blog/tags` 返回成功。
- 只读核验该库有 23 条 Note，满足已发布、未删除且无密码条件的文章为 0。
- 页面空状态符合真实数据；没有自动发布笔记、迁移文章或写入演示数据。
- 主前端 `http://localhost:3002`，主后端 `http://localhost:4000`；根命令 `pnpm dev:blog:full`。

## 浏览器核验

使用临时进程内 HTTP 数据检查有内容的页面，未连接数据库，未添加测试文件。
真实 3002 站点始终读取主后端，临时页面在独立端口运行。

- 1440×900：首页左右布局、文章列表、独立阅读目录、浅色与深色截图检查。
- 768×1024：首页与列表的平板布局检查。
- 390×844：手机首页、文章列表、带图搜索结果、阅读页、目录和图片弹层检查。
- 以上布局的 DOM 宽度检查均未发现横向页面溢出。
- 手机搜索得到对应文章，进入详情后的返回链接保留搜索条件。
- 目录选择章节后关闭弹层，恢复触发按钮焦点，并更新文章锚点。
- 图片能够放大并通过 Esc 关闭；行内链接预览能取得网页摘要。
- 切换黑体与深浅色后，正文、链接浮层、标签与工具控件共用主题。
- 根据截图将手机大封面改为小缩略图，移除手机首页重复标识，并调整标签／日期分组。

## 对比度与工程核验

浅色文字相对奶白背景：正文 12.75、次级文字 5.74、辅助文字 4.59、链接 5.21。
深色对应值：13.46、8.45、6.56、8.05；标签和主按钮文字均超过 4.5。

- 博客 ESLint、TypeScript、生产构建通过，主服务 TypeScript 通过。
- `src` 内 TS / TSX / CSS 文件均不超过 200 行。
- 主题文件外未发现颜色字面量，CSS 语义变量没有未定义引用。
- 保留服务端 Markdown、按意图预取、按需链接摘要和减少动态效果设置；没有新增依赖。
