# 变更记录 - 2026-09-18

> 本文件记录当日 UI 预览变更；各项验证结果见对应记录。

---

## [09:05] commit: `383dc5e2048b4548596b2592832cddd8ef03e1e7` - style(auth): 替换认证页高清背景
**提交者**: ChatGPT
**变更文件**:
- `client/public/brand/auth-workspace-hq.webp`（新建）
- `client/public/brand/auth-workspace.md`（修改）
- `client/src/views/login/AuthShell.tsx`（修改）

**Review 结果**: ✅ 提交前静态审查、CI lint/build 与 8088 Preview Deploy 均通过；最终视觉仍以实际浏览器为准。

### 变更摘要
- 使用用户最后确认的“暖阳下的创意工作角落”作为认证页背景，不重新生成或改动代码级 Nubbi Logo。
- 将旧的低码率 AVIF 页面引用切换为 WebP quality=95 资源；保持 1672 × 941 尺寸，资源约 196 KB，以降低全屏 cover 场景下的压缩发糊。
- 保持背景裁切位置、登录卡布局、100dvh 无滚动约束和短屏适配规则不变。
- 不修改登录、注册、邮箱验证、密码重置、路由或 Better-Auth 业务逻辑。


---

## [10:20] commit: `未提交` - style(client): 增加认证卡片叠纸与波浪交互
**提交者**: ChatGPT
**变更文件**:
- `client/src/views/login/AuthShell.tsx`（修改）
- `client/src/views/login/auth-shell.css`（修改）
- `client/src/views/login/index.tsx`（修改）

**Review 结果**: ⚠️ 提交前静态审查通过；CI、8088 与移动端视觉待部署后验证。

### 变更摘要
- 保持现有高清背景、固定 Nubbi Logo、100dvh 无滚动和 Better-Auth 逻辑不变。
- 登录卡新增双层旋转纸张结构；hover 只使用 transform / opacity 做轻微上浮、旋转与后层纸张视差。
- 顶部 DAILY NOTES 纸签加入 hover 抬起与逐字波浪效果；短屏和 reduced-motion 会自动关闭非必要动效。
- 登录 / 注册 / 找回密码切换时，标题与内容使用短时纸张入场动画，后层纸张随状态轻微换位。
- 登录表单改为邮箱密码主流程优先；“忘记密码”与密码标签同行；GitHub 登录缩为独立圆形图标，仅做普通 hover 缩放。
- 输入框 focus 增加轻量底边强调；主按钮仅增加 hover 上浮与按压缩放，不引入新动画依赖。


---

## [02:51] commit: `未提交` - fix(client): 回退认证卡片交互动效
**提交者**: ChatGPT
**变更文件**:
- `client/src/views/login/AuthShell.tsx`（回退）
- `client/src/views/login/auth-shell.css`（回退）
- `client/src/views/login/index.tsx`（回退）

**Review 结果**: ✅ 回退到已通过 Preview Deploy 的稳定版本；等待本次 CI / 8088 再验证。

### 变更摘要
- 回退 PR #10 引入的叠纸、纸签波浪、表单重排与状态切换动效。
- 恢复到高清背景提交 `383dc5e2048b4548596b2592832cddd8ef03e1e7` 对应的稳定登录页结构。
- 保留高清背景、100dvh 无滚动约束、固定 Nubbi Logo 与 Better-Auth 业务逻辑。
- 后续交互动效改为逐项引入，不再一次性修改卡片结构与表单层级。


---

## [10:58] commit: `未提交` - style(client): 重构认证卡片动效为稳定语义结构
**提交者**: ChatGPT
**变更文件**:
- `client/src/views/login/AuthShell.tsx`（修改）
- `client/src/views/login/auth-shell.css`（重构）
- `client/src/views/login/index.tsx`（修改）

**Review 结果**: ⚠️ 提交前结构审查通过；CI、8088 与手机视觉待部署验证。

### 变更摘要
- 将认证页短屏适配从 first-child / nth-of-type / :has 等 DOM 位置选择器改为显式 auth-* 语义 class，降低后续 UI 重排导致布局崩坏的风险。
- 在 AuthShell 外层加入不参与布局计算的双层叠纸伪元素；主卡仅做 2px 上浮与极小旋转，不改变表单尺寸。
- DAILY NOTES 标签使用绝对定位，不占据卡片流式高度；hover 才触发一次逐字波浪，短屏自动隐藏。
- 登录 / 注册 / 找回密码仅对 Header 和 Content 做短时入场动画，不做 DOM 翻页重构。
- GitHub 登录缩为圆形图标按钮，仅做 hover scale；忘记密码与密码 Label 同行。
- 保持高清背景、固定 NubbiBrand、100dvh 无滚动和 Better-Auth 业务流程。


---

## [11:10] commit: `未提交` - style(client): 恢复原始登录卡片样式
**提交者**: ChatGPT
**变更文件**:
- `client/src/views/login/AuthShell.tsx`（恢复）
- `client/src/views/login/auth-shell.css`（恢复）
- `client/src/views/login/index.tsx`（恢复）

**Review 结果**: ✅ 代码恢复到高清背景版本的已验证认证卡片结构；等待本次 Preview Deploy 再确认 8088。

### 变更摘要
- 移除叠纸、DAILY NOTES、波浪文字、卡片 hover/3D、GitHub 圆形图标与认证状态入场动画。
- 恢复高清背景提交 `383dc5e2048b4548596b2592832cddd8ef03e1e7` 对应的原始白色登录卡片、整行 GitHub 登录按钮与原始表单排版。
- 保留高清工作台背景、固定 NubbiBrand、100dvh 无滚动/无溢出规则和 Better-Auth 业务逻辑。


---

## [11:24] commit: `未提交` - style(client): 将 GitHub 登录收为图标按钮
**提交者**: ChatGPT
**变更文件**:
- `client/src/views/login/index.tsx`（修改）

**Review 结果**: ✅ 仅修改登录页 GitHub 入口表现，不修改认证逻辑、全局主题或响应式规则；等待 Preview Deploy 验证。

### 变更摘要
- 将整行“使用 GitHub 登录”按钮改为居中的圆形 GitHub 图标按钮。
- 保留 `handleGitHubLogin`、禁用态、可访问性标签与原有邮箱登录分隔结构。
- 不修改 AuthShell、auth-shell.css、theme.css、公共 Button/Input/Card 或 Better-Auth 逻辑。
