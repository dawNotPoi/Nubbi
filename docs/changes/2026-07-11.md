# 变更记录 - 2026-07-11

> 当日统计：6 次提交 | 132 文件变更 | 通过率 50%

---

## [08:22] commit: `3fbf67b` - 新增 MCP 笔记集成与回收站

**提交者**: weixiaoing
**变更文件**:
- `mcp/**`（新建 MCP Server、工具、传输层、测试与部署配置）
- `server/app/controller/mcp/**`、`server/app/routes/mcp/**`（新建 MCP API）
- `server/app/middleware/**`、`server/app/lib/**`（修改鉴权与 MCP 权限策略）
- `server/app/controller/note/**`、`server/app/routes/note.ts`（修改笔记访问控制与结构锁）
- `client/src/features/api-token/**`（新建 API Token 用途管理）
- `client/src/features/note-trash/**`、`client/src/views/note-trash/**`（新建回收站界面）
- `package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`docker-compose.yml`（修改 workspace 与部署配置）
- `docs/**`（修改 MCP、鉴权、笔记与基础设施文档）

**Review 结果**: ⚠️ 警告

### 变更摘要
- 新增支持 stdio 与 Streamable HTTP 的 Nubbi MCP Server，并提供受限的笔记读写工具。
- 新增 MCP Agent Token、服务端权限边界、内容 revision 校验和笔记结构租约锁。
- 新增笔记回收站页面、批量恢复与永久删除流程，并重构 API Token 管理界面。
- 清理 225 个纯换行符变更；暂存差异通过 `git diff --check`。
- 静态审查未发现明确阻断问题；测试、类型检查和构建因本地 `node_modules` 链接损坏且依赖重装未完成而未通过验证。

---

## [21:46] commit: `未提交` - 优化登录页交互与认证流程

**提交者**: weixiaoing
**变更文件**:
- `client/src/views/login/index.tsx`（修改：验证码发送/冷却统一、表单校验补齐、form 化与可访问性、提示去重）
- `client/src/component/UI/password-input.tsx`（新建：带可见性切换的密码输入框）
- `client/src/utils/auth.ts`（修改：resendVerificationCode 返回结构、authorizedFetch 401 刷新重试；删除死代码）
- `client/src/api/request.ts`（修改：删除无引用的 getWebData）
- `client/src/main.tsx`（修改：message 全局 maxCount=3）
- `client/src/views/reset-password/index.tsx`（修改：密码长度规则 6→8）
- `client/src/component/auth/AuthCodeForm.tsx`（删除：零引用组件）
- `server/app/routes/auth.ts`、`server/app/lib/auth.ts`（修改：密码长度校验统一至 8 位，显式 minPasswordLength）

**Review 结果**: ✅ 通过

### 变更摘要
- 登录遇"邮箱未验证"时利用 better-auth 登录自动发码的行为，验证页正确显示 60s 冷却与动态标题；"重新发送验证码"补上冷却倒计时，撞服务端冷却时按"已发送"接上剩余秒数。
- 密码长度前后端统一至 8 位（前端校验、路由校验、better-auth minPasswordLength），消除 6-7 位密码通过校验却烧掉验证码再失败的问题；注册提交补齐用户名/密码前端校验。
- verifyEmail/forgotPassword 视图表单化支持回车提交、验证码框自动聚焦；修复注册页"获取验证码"按钮缺 type="button" 误触发表单提交的 bug；5 处密码框支持可见性切换；补齐 id/name/htmlFor/autoComplete/aria 可访问性属性。
- 提示统一：字段级错误只用 inline、第三方登录回调错误只留 banner，不再双弹 toast。
- authorizedFetch 遇 401 先强制刷新会话并重试一次，仍失败才登出跳转；清理零引用死代码。
- 验证：client tsc/eslint、server tsc + 7 项单测均通过（本机 node 无法解析 Linux pnpm 符号链接，在 remote-ai-dev 容器内执行）。
