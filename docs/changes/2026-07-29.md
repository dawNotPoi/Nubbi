# 变更记录 - 2026-07-29

> 当日统计：2 次提交 | 39 文件变更 | 通过率 50%

---

## [16:14] commit: `7096d4c` - 简化 Note 状态并修复本地登录

**提交者**: dawn
**变更文件**:
- `client/src/component/{MobileNavigation,SideBar}/**`（修改：适配无用户维度的 Note 缓存作用域，重新设计新建笔记弹窗）
- `client/src/features/note/**`、`client/src/store/atom/noteAtom.ts`（修改：简化 Note 列表 Query Key、缓存和 mutation 参数）
- `client/src/hooks/useAuth.ts`（修改：登出和账号注销后清空 Query Cache）
- `server/app/lib/{auth,trusted-origins}.ts`、`docs/auth/PRD.md`（修改：开发环境动态信任本机前端实际端口）
- `server/app/controller/{note,mcp,summary}.ts`、`server/app/services/note/purge.ts`（修改：移除 Note 结构租约锁包装）
- `server/app/models/noteStructureLock.ts`、`server/app/services/note/structure-lock.ts`（删除：不再使用的租约锁模型与服务）
- `docs/note/PRD.md`（修改：记录 Note 缓存作用域和无服务端结构锁约定）

**Review 结果**: ⚠️ 警告

### 变更摘要
- Note 列表缓存不再包含用户 ID，退出登录和账号注销时统一清空 Query Cache，减少无效的 owner 参数传递。
- 保留新建笔记弹窗的大尺寸编辑区域，将顶部恢复为紧凑的存放位置选择样式。
- 修复 Vite 自动切换到 5174 等端口时 GitHub 登录被 CORS 和 Better Auth Origin 校验拒绝的问题；生产环境仍使用精确白名单。
- 移除 Note 用户级 Mongo 租约锁及其 2 秒等待 409，保留删除、恢复、移动和永久删除原有业务校验。
- 前后端 TypeScript 与 `git diff --check` 通过；ESLint 为 0 errors、18 warnings，现有 Fast Refresh、Hook 依赖和 `any` 告警未在本次扩展处理。

## [16:42] commit: `未提交` - 优化笔记删除导航与侧栏浮层

**提交者**: dawn
**变更文件**:
- `client/src/features/note/hooks/useDeleteNote.ts`（新增：统一删除笔记后的导航处理）
- `client/src/component/SideBar/NoteMenu/**`（修改：菜单删除接入统一删除 Hook）
- `client/src/component/SideBar/NoteDnd/**`（修改：拖入回收站接入统一删除 Hook，并拆分拖拽模型与放置区域）
- `client/src/component/SideBar/ResizeTab/index.tsx`、`client/src/component/SideBar/index.tsx`（修改：增加桌面端收起后的固定宽度悬浮侧栏）
- `docs/note/PRD.md`、`docs/infrastructure/PRD.md`（修改：记录删除后导航和悬浮侧栏交互）

**Review 结果**: ✅ 通过

### 变更摘要
- 删除当前笔记或其祖先成功后返回主页，删除失败时保留当前页面；侧边栏菜单和拖入回收站共用同一处理。
- 收起侧边栏后，鼠标移入左侧热区显示带间距、圆角和阴影的固定宽度浮层，仅使用透明度与位移动效，不触发宽度渐变。
- 客户端 TypeScript、相关文件 ESLint、生产构建和 `git diff --check` 均通过。
