# 变更记录 - 2026-09-11

> 当日统计：3 次提交 | 275 个变更路径 | 工作区解析验证通过

---

## [14:00] commit: `193c798` - chore: 移除 assistant 并独立为单独仓库

**提交者**: dawn

**变更文件**:
- assistant/**（删除，241 个文件，含 api、web、mobile、shared、skills、mcp、config）

**Review 结果**: ✅ 通过

### 变更摘要

- assistant 迁移为独立仓库，本仓库不再跟踪其源码，避免两个仓库重复维护。

### 验证与边界

- 本次仅移除被跟踪文件，统计为 241 个删除、17289 行删除，`git status` 干净。
- 此时 `pnpm-workspace.yaml`、`package.json` 等仍引用 assistant，由下一条记录清理。

---

## [14:05] commit: `e391a6c` - chore(config): 清理 assistant 残留的工作区与脚本引用

**提交者**: dawn

**变更文件**:
- .agent/module-index.md（修改）
- .gitignore（修改）
- package.json（修改）
- pnpm-lock.yaml（修改）
- pnpm-workspace.yaml（修改）
- docs/changes/2026-09-11.md（新建）

**Review 结果**: ✅ 通过

### 变更摘要

- `pnpm-workspace.yaml` 移除 `assistant/api`、`assistant/web`、`assistant/mobile`、`assistant/shared` 四个工作区条目。
- `package.json` 移除 `dev:assistant`、`dev:assistant:api`、`dev:assistant:web`、`dev:assistant:mobile`、`build:assistant` 脚本。
- `.gitignore` 删除 assistant 专属忽略规则，保留与 assistant 无关的 `/data/codex/`。
- `.agent/module-index.md` 删除 assistant 模块映射行，该行指向的 `docs/assistant/` 已不存在。
- `pnpm-lock.yaml` 用 `pnpm install --lockfile-only` 重新生成：删除 4 个 assistant importer 及其独占依赖子树（562 个包名），并因 assistant 的 eslint 依赖退出而重新接线 website 的 eslint/typescript-eslint peer 上下文，非 assistant 依赖版本未升级。

### 验证与边界

- `pnpm -r list --depth -1` 只剩 nubbi、nubbi-client、nubbi-mcp-server、nubbi-server、nubbi-website 五个包。
- 全仓库检索（排除 `docs/changes` 历史记录、`node_modules`、`.tmp`）已无 assistant 引用。
- `node_modules` 里 assistant 的旧软链未处理，下次完整 `pnpm install` 会自动清理。

---

## [14:50] commit: `未提交` - feat(shared): 优化文件页交互并新增存储用量展示

**提交者**: dawn
**变更文件**:
- client/src/api/file.ts（修改）
- client/src/component/upload/UploadItem.tsx（修改）
- client/src/component/upload/UploadLifecycle.tsx（修改）
- client/src/component/upload/hooks/GlobalUpload.ts（修改）
- client/src/features/file/components/FileBreadcrumbs.tsx（修改）
- client/src/features/file/components/FileList.tsx（修改）
- client/src/features/file/components/FileQuota.tsx（新建）
- client/src/features/file/components/FileRow.tsx（修改）
- client/src/features/file/components/FileRowActions.tsx（修改）
- client/src/features/file/components/FileToolbar.tsx（修改）
- client/src/features/file/components/FileUploadButton.tsx（修改）
- client/src/features/file/components/fileRowMenu.tsx（新建）
- client/src/features/file/dropFiles.ts（新建）
- client/src/features/file/hooks/useFileManagerActions.ts（修改）
- client/src/features/file/hooks/useFileManagerController.ts（修改）
- client/src/features/file/model.ts（修改）
- client/src/features/file/preferences.ts（新建）
- client/src/features/upload/Uploader.ts（修改）
- client/src/features/upload/model.ts（修改）
- client/src/store/atom/FileAtom.ts（修改）
- client/src/theme.css（修改）
- client/src/views/file-manage/components/FilePreviewModal.tsx（修改）
- client/src/views/file-manage/index.tsx（修改）
- docs/file/PRD.md（修改）
- server/app/controller/file/management.ts（修改）
- server/app/routes/file/management.ts（修改）
- server/app/services/fileManagement/stats.ts（新建）
- server/app/services/fileUpload/reservation.ts（修改）

**Review 结果**: ⚠️ 警告

### 变更摘要

- 新增只读接口 `GET /file/stats`，返回活跃文件占用、上传中预留、配额与文件/文件夹数量；配额校验与用量展示共用 `getStorageUsage` 统计口径。
- 文件页标题下方新增用量条，删除与上传完成后自动刷新；上传面板逐项显示目标目录（含断点恢复会话）。
- 文件名与扩展名分开渲染，主名省略时扩展名保持可见；搜索命中高亮；时间悬停显示完整时间戳；大小与时间等宽对齐。
- 桌面端支持点击列头排序、Shift 连续选择、Ctrl/Cmd 点选、右键上下文菜单与完整键盘操作（方向键、Home/End、Enter、空格、Delete、Ctrl/Cmd+A、Esc）。
- 支持拖拽移动（拖到文件夹行或面包屑，多选整体拖拽）与拖拽上传（文件或文件夹拖入列表/文件夹行/面包屑，递归展开上限 100 个）。
- 预览支持 ←/→ 在同目录可预览文件间切换并显示位置；面包屑超过 4 级折叠中间层级；空目录与筛选无结果使用不同入口；排序与类型筛选本地持久化。
- 保留 PRD 既有约束：普通文件和已有文件夹仍不提供重命名入口；未改动存储落盘、目录结构与数据模型。

### 验证与边界

- 服务端 `tsc --noEmit`、改动文件 ESLint、`git diff --check` 通过；客户端生产构建通过（仅既有大包警告）。
- 客户端全量 `tsc -b` 仍被笔记模块已有的 `UpdateNoteContentVariables` 重复声明阻塞，本批未修改笔记模块。
- 拖拽、键盘导航、右键菜单与预览切换仅完成代码级审查，本机无浏览器环境，未做浏览器交互验证。
