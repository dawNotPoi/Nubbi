# 变更记录 - 2026-07-13

> 当日统计：3 次提交 | 97 文件变更 | 通过率 100%

---

## [11:45] commit: `d116004` - 主页侧边栏笔记树新增拖拽移动

**提交者**: dawn
**变更文件**:
- `client/src/component/SideBar/NoteDnd/NoteDndProvider.tsx`（新建：DndContext 提供器、拖拽状态与 drop 处理、DragOverlay、根区域/回收站 drop zone）
- `client/src/component/SideBar/NoteDnd/useNoteDropTarget.ts`（新建：节点级 droppable、三态高亮、悬停 700ms 自动展开）
- `client/src/component/SideBar/NoteMenu/NoteTree.tsx`（修改：树节点接入 draggable/droppable）
- `client/src/component/SideBar/NoteMenu/index.tsx`（修改：“小记”标题与树底部根区域 drop zone）
- `client/src/component/SideBar/index.tsx`（修改：挂载 NoteDndProvider、回收站 drop target）
- `client/src/store/atom/noteAtom.ts`（修改：移动后旧父 children 为空时同步清除 hasChildren）
- `client/package.json`、`pnpm-lock.yaml`（新增依赖 @dnd-kit/core@6.3.1）

**Review 结果**: ✅ 通过

### 变更摘要
- 侧边栏笔记树支持拖拽移动层级：拖到其他笔记上成为其子笔记（目标自动展开），拖到“小记”标题或树底部虚线提示条移至根级，拖到回收站入口软删除；复用现有 `PUT /note/properties`、软删除 API 及乐观更新/失败回滚。
- 非法落点（自身/后代）显示红色禁止样式，drop 时警告“不能移动到自身或其子级”且不发请求；拖回当前父级静默 no-op；PointerSensor 8px 激活阈值保证点击跳转、展开箭头、hover 按钮不受影响。
- 修复：拖走某节点最后一个子笔记后，旧父的展开箭头立即消失（旧父 hasChildren 缓存同步）。
- 验证：client `tsc -b` 与 eslint 通过（0 errors）；浏览器实测移动到子级、非法落点拦截、移至根级、hasChildren 修复均通过。

## [16:44] commit: `未提交` - 完成全客户端移动端适配

**提交者**: dawn
**变更文件**:
- `client/src/Route.tsx`、`client/src/component/Header.tsx`、`client/src/component/SideBar/**`、`client/src/store/atom/common.ts`（修改：响应式应用壳与移动抽屉）
- `client/src/component/MobileNavigation.tsx`、`client/src/hooks/useIsMobile.ts`（新建：移动底栏和断点状态）
- `client/src/views/home/**`、`client/src/views/NoteLibrary.tsx`、`client/src/features/note/components/**`、`client/src/views/note/**`（修改：首页、笔记库与编辑器移动布局）
- `client/src/views/file-manage/**`、`client/src/component/upload/UploadListWrapper.tsx`（修改：移动文件列表、面包屑、预览和上传面板）
- `client/src/views/meetings/**`、`client/src/views/meeting-room/**`、`client/src/component/MeetingList/**`（修改：会议列表及会议室横竖屏布局）
- `client/src/component/UI/Dialog/index.tsx`、`client/src/index.css`、登录与重置密码页面（修改：共享弹窗、安全区和触控适配）
- `docs/{infrastructure,home,note,file,meeting}/PRD.md`（修改：补充移动端行为约定）

**Review 结果**: ✅ 通过

### 变更摘要
- `768px` 以下使用「首页 / 笔记 / 新建 / 文件 / 更多」底部导航，桌面侧栏改为移动抽屉并与桌面持久化状态隔离。
- 笔记库、编辑器、文件管理、会议与回收站改为触控友好的单列布局；Markdown 分屏在手机上改为源码/预览切换，上传和会议评论等浮层改为底部面板。
- 视频会议室移除固定最小宽度，成员、评论与控制栏适配竖屏、横屏和系统安全区。
- 验证：生产构建、TypeScript、ESLint 和 `git diff --check` 通过；实测 360/390/768/1440px 视口无页面级横向溢出。

## [17:53] commit: `未提交` - 优化文件分片上传与断点续传

**提交者**: dawn
**变更文件**:
- `server/app/services/fileUpload/**`、`server/app/controller/fileUpload.ts`、`server/app/routes/fileUpload.ts`（新增：上传初始化、分片校验、原子合并、任务状态、取消与定时清理）
- `server/app/models/file/{file,uploadTask}.ts`、`server/app/routes/{file,auth}.ts`、`server/app/{index,lib/env,middleware/common}.ts`、`server/.example.env`（修改：按用户的 hash + size 秒传、数字容量字段、配额与任务生命周期配置）
- `client/src/features/upload/**`、`client/src/component/upload/{UploadLifecycle,UploadItem,UploadListWrapper}.tsx`、`client/src/component/upload/hooks/**`（新增/修改：并发上传、自动重试、暂停恢复、刷新后续传与任务操作）
- `client/src/{api,store,utils}/**`、`client/src/{AppProvider,component/MobileNavigation,component/SideBar/index,views/file-manage/index}.tsx`（修改：上传 API 错误处理、全局任务状态、移动端入口徽标和多文件选择）
- `server/test/fileUpload{Storage,Validation}.test.ts`、`docs/file/{PRD,bugs}.md`（新增/修改：分片合并与参数验证测试、上传约束和问题状态）

**Review 结果**: ✅ 通过

### 变更摘要
- 秒传限定为同一用户，并以 `ownerId + hash + size` 匹配；保留现有全量/采样 MD5 计算策略，通过独立文件大小字段降低误碰撞风险。
- 上传任务使用服务端 `uploadId` 隔离临时目录，校验分片序号和大小，采用 `.part` 文件原子落盘；支持任务查询、取消、24 小时过期清理和幂等合并。
- 客户端支持单文件 3 分片、全局 6 分片并发，网络/限流/服务端错误退避重试，暂停、继续、失败重试、多文件选择和刷新后重新选择原文件续传。
- 默认限制为单文件 10GB、单用户 100GB、最多 5 个未完成任务，可通过环境变量调整。
- 验证：服务端 13 项测试、前后端 TypeScript、客户端生产构建、定向 ESLint（0 error）和 `git diff --check` 通过；当前环境缺少应用内浏览器，交互回归留待人工测试。
