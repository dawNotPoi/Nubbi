# 变更记录 - 2026-09-10

> 提交前统计：3 批变更合并提交 | 111 个业务文件变更（不含本记录）

---

## [10:32] commit: `375b34d` - feat(client): 完善视频会议入会准备与交互体验

**提交者**: dawn
**变更文件**:
- client/src/api/meeting.ts（修改）
- client/src/component/MeetingList/RecentMeeting.tsx（修改）
- client/src/component/MeetingList/create-meeting-modal.tsx（修改）
- client/src/component/MeetingList/meeting-manage/meeting-card.tsx（修改）
- client/src/component/MeetingList/meeting-manage/meeting-comments-modal.tsx（修改）
- client/src/hooks/meeting/p2p/socket-events.ts（修改）
- client/src/hooks/meeting/p2p/types.ts（修改）
- client/src/hooks/meeting/p2p/use-peer-manager.ts（修改）
- client/src/hooks/meeting/p2p/use-socket-actions.ts（修改）
- client/src/hooks/useMedia.ts（删除，职责迁入会议媒体模块）
- client/src/hooks/useP2PConnection.ts（修改）
- client/src/views/meeting-room/MeetingAccessGuard.tsx（修改）
- client/src/views/meeting-room/components/CommentPanel.tsx（修改）
- client/src/views/meeting-room/components/MainVideoStage.tsx（修改）
- client/src/views/meeting-room/components/MeetingRoomView.tsx（修改）
- client/src/views/meeting-room/components/ParticipantSidebar.tsx（修改）
- client/src/views/meeting-room/components/VideoControls.tsx（修改）
- client/src/views/meeting-room/helpers/meeting-room.ts（修改）
- client/src/views/meeting-room/hooks/use-comment-composer.ts（修改）
- client/src/views/meeting-room/hooks/use-meeting-session.ts（修改）
- client/src/views/meeting-room/index.tsx（修改）
- client/src/views/meeting-room/types.ts（修改）
- docs/meeting/PRD.md（修改）
- client/src/features/meeting/invitation.ts（新建）
- client/src/features/meeting/meeting-invitation.tsx（新建）
- client/src/hooks/meeting/p2p/peer-tracks.ts（新建）
- client/src/hooks/meeting/p2p/request-acknowledgement.ts（新建）
- client/src/views/meeting-room/active-meeting-room.tsx（新建）
- client/src/views/meeting-room/components/media-device-control.tsx（新建）
- client/src/views/meeting-room/components/meeting-exit-dialog.tsx（新建）
- client/src/views/meeting-room/components/meeting-lobby.tsx（新建）
- client/src/views/meeting-room/components/remote-audio.tsx（新建）
- client/src/views/meeting-room/hooks/use-audio-level.ts（新建）
- client/src/views/meeting-room/hooks/use-local-media.ts（新建）
- client/src/views/meeting-room/hooks/use-room-actions.ts（新建）
- client/src/views/meeting-room/media/device-capture.ts（新建）
- client/src/views/meeting-room/media/media-state.ts（新建）
- client/src/views/meeting-room/media/screen-capture.ts（新建）
- docs/meeting/meeting-experience.excalidraw（新建）

**Review 结果**: ⚠️ 警告

### 变更摘要

- 新增入会准备页，分别申请摄像头和麦克风权限，支持设备预览、音量检测、权限恢复与仅收听加入；准备期间不建立会议连接。
- 将旧 useMedia 拆为会议域的媒体状态、设备采集和屏幕采集；切换设备保留开关，离会及卸载释放轨道，迟到权限结果不恢复采集。
- 所有远端音频独立播放，主画面及缩略图静音；浏览器阻止自动播放时提供恢复入口。
- 统一处理 Peer 轨道新增、替换和移除；重新连接使用新 Socket 身份，旧连接信令和关闭回调不覆盖新连接。
- 增加连接及重新入会状态、请求超时和断线反馈；聊天及结束操作不自动重发，发送结果不确定时保留草稿并提示检查记录。
- 主持人支持仅自己离开，结束所有人增加二次确认；创建会议失败保留表单，成功后显示邀请入口。
- 邀请使用当前站点链接且不包含密码或入会凭证；会议列表、创建成功后和会中均可打开，复制失败支持手动复制。
- 统一控制按钮动作文案，桌面成员栏可收起，移动端核心操作自动换行，聊天及成员面板不遮挡底部操作；中文输入法确认不误发消息。
- 会议历史评论改用记录 ID 作为列表 key，修复原有日期类型报错；更新会议 PRD 和流程图。

### 验证与边界

- 前端生产构建通过；仍有主包超过 2000 kB 的体积警告。
- 服务端类型检查通过；本批变更文件 ESLint 和 git diff --check 通过。
- 15 项仓库外离线模拟通过，涵盖权限拒绝、设备切换、迟到授权、停止共享、轨道同步、断线及超时。
- 隔离浏览器页面验证了准备页、权限失败后仅收听、成员栏开关、邀请链接、二次结束确认及 390px 手机布局。
- 音频组件使用模拟播放器验证两个纯音频远端成员独立播放、摄像头状态变化不影响声音及成员退出清理。
- 36 份新增或修改源码均不超过 200 行；未向仓库新增测试文件、临时验证脚本或本地凭据。
- 前端全量类型检查仍被已有的 client/src/features/note/model/types.ts 中两处 UpdateNoteContentVariables 重复声明阻塞；已核对 HEAD 中存在，本批不修改笔记模块。
- 尚未验证真实多设备、跨网络通话及实际音视频质量；本轮未部署 TURN、未修改数据库协议，不含会议笔记、录制和 AI 纪要。

## [11:38] commit: `375b34d` - feat(shared): 完善视频会议体验与连接恢复并接入TURN部署

**提交者**: dawn
**变更文件**:
- .github/scripts/render-deploy-env.cjs（本次提交；包含前批交互变更）
- client/src/features/meeting/invitation.ts（本次提交；包含前批交互变更）
- client/src/features/meeting/meeting-invitation.tsx（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/connection-types.ts（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/peer-network.ts（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/peer-session.ts（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/peer-tracks.ts（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/request-acknowledgement.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/active-meeting-room.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/connection-diagnostics.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/media-device-control.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/meeting-exit-dialog.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/meeting-lobby.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/remote-audio.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/hooks/use-audio-level.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/hooks/use-local-media.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/hooks/use-room-actions.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/media/device-capture.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/media/media-state.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/media/screen-capture.ts（本次提交；包含前批交互变更）
- docs/meeting/connection-recovery.excalidraw（本次提交；包含前批交互变更）
- docs/meeting/discussion.md（本次提交；包含前批交互变更）
- docs/meeting/meeting-experience.excalidraw（本次提交；包含前批交互变更）
- scripts/deploy-docker-support.sh（本次提交；包含前批交互变更）
- scripts/turn/configure.mjs（本次提交；包含前批交互变更）
- server/app/services/meeting/ice-configuration.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/comment-snapshot.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/member-recovery.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/peer-negotiation-state.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/peer-negotiation.ts（本次提交；包含前批交互变更）
- turn/.dockerignore（本次提交；包含前批交互变更）
- turn/.example.env（本次提交；包含前批交互变更）
- turn/Dockerfile（本次提交；包含前批交互变更）
- turn/README.md（本次提交；包含前批交互变更）
- turn/certs/.gitkeep（本次提交；包含前批交互变更）
- turn/start.sh（本次提交；包含前批交互变更）
- .dockerignore（本次提交；包含前批交互变更）
- .github/scripts/deploy-service.sh（本次提交；包含前批交互变更）
- .github/scripts/detect-service-changes.sh（本次提交；包含前批交互变更）
- .gitignore（本次提交；包含前批交互变更）
- client/src/api/meeting.ts（本次提交；包含前批交互变更）
- client/src/component/MeetingList/RecentMeeting.tsx（本次提交；包含前批交互变更）
- client/src/component/MeetingList/create-meeting-modal.tsx（本次提交；包含前批交互变更）
- client/src/component/MeetingList/meeting-manage/meeting-card.tsx（本次提交；包含前批交互变更）
- client/src/component/MeetingList/meeting-manage/meeting-comments-modal.tsx（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/socket-events.ts（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/types.ts（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/use-peer-manager.ts（本次提交；包含前批交互变更）
- client/src/hooks/meeting/p2p/use-socket-actions.ts（本次提交；包含前批交互变更）
- client/src/hooks/useMedia.ts（本次提交；包含前批交互变更）
- client/src/hooks/useP2PConnection.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/MeetingAccessGuard.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/CommentPanel.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/MainVideoStage.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/MeetingRoomView.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/ParticipantSidebar.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/components/VideoControls.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/helpers/meeting-room.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/hooks/use-comment-composer.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/hooks/use-meeting-session.ts（本次提交；包含前批交互变更）
- client/src/views/meeting-room/index.tsx（本次提交；包含前批交互变更）
- client/src/views/meeting-room/types.ts（本次提交；包含前批交互变更）
- docker-compose.yml（本次提交；包含前批交互变更）
- docs/deployment.md（本次提交；包含前批交互变更）
- docs/infrastructure/PRD.md（本次提交；包含前批交互变更）
- docs/meeting/PRD.md（本次提交；包含前批交互变更）
- package.json（本次提交；包含前批交互变更）
- scripts/deploy-docker.sh（本次提交；包含前批交互变更）
- server/.example.env（本次提交；包含前批交互变更）
- server/app/index.ts（本次提交；包含前批交互变更）
- server/app/lib/env.ts（本次提交；包含前批交互变更）
- server/app/socket/authentication.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/index.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/membership-events.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/room-state.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/schemas.ts（本次提交；包含前批交互变更）
- server/app/socket/meeting/types.ts（本次提交；包含前批交互变更）

**Review 结果**: ⚠️ 警告

### 变更摘要

- 合并提交前批入会准备、媒体设备、邀请、退出确认和独立远端音频播放等交互改动。
- 增加连接代次与客户端会话身份校验、入会和历史评论读取分离、有限重试与短断恢复；旧信令及过期回调不覆盖新连接。
- 服务端签发 TURN 短期凭证，客户端支持 ICE 配置和连接诊断。
- 新增项目内 coturn 容器、本地初始化与校验命令，共享独立 TURN 环境配置；地址显式传入，随机密钥不覆盖已有配置。
- 接入 Compose 和现有后端部署流程；发布包排除密钥及证书，切换发布目录时保留服务器配置，补充部署与会议文档。

### 验证与边界

- 前批前端生产构建、会议改动 ESLint 及隔离交互验证通过；连接恢复的 21 项隔离验证通过（真实本地 Socket.IO 与模拟 RTC）。
- 本轮服务端类型检查、Compose 静态配置、部署脚本语法及 20 项 TURN 隔离配置校验通过。
- 前端全量类型检查仍存在原有笔记模块 UpdateNoteContentVariables 重复声明；生产构建有既有大包警告。
- Docker Linux 引擎未运行，未构建或启动 TURN 容器；真实多设备、跨网络和音视频中继尚未验证。不将配置检查等同于中继可用。
- 未提交真实环境文件、私钥、仓库外临时校验脚本；仅本地提交，不推送或远程部署。

## [17:00] commit: `未提交` - feat(shared): 优化会议聊天、连接提示与画面布局

**提交者**: dawn
**变更文件**:
- .agent/rules/core.md（修改）
- .agent/skills/review.md（修改）
- AGENTS.md（修改）
- client/src/hooks/meeting/p2p/connection-types.ts（修改）
- client/src/hooks/meeting/p2p/peer-network.ts（修改）
- client/src/hooks/meeting/p2p/peer-session.ts（修改）
- client/src/hooks/meeting/p2p/types.ts（修改）
- client/src/views/meeting-room/active-meeting-room.tsx（修改）
- client/src/views/meeting-room/components/CommentComposer.tsx（修改）
- client/src/views/meeting-room/components/CommentPanel.tsx（修改）
- client/src/views/meeting-room/components/MainVideoStage.tsx（修改）
- client/src/views/meeting-room/components/MeetingRoomView.tsx（修改）
- client/src/views/meeting-room/components/ParticipantSidebar.tsx（修改）
- client/src/views/meeting-room/components/ParticipantTile.tsx（修改）
- client/src/views/meeting-room/components/VideoControls.tsx（修改）
- client/src/views/meeting-room/components/connection-diagnostics.tsx（修改）
- client/src/views/meeting-room/components/participant-media.tsx（新建）
- client/src/views/meeting-room/hooks/use-comment-composer.ts（修改）
- client/src/views/meeting-room/hooks/use-comment-scroll.ts（新建）
- client/src/views/meeting-room/hooks/use-meeting-chat.ts（新建）
- client/src/views/meeting-room/hooks/use-meeting-participants.ts（修改）
- client/src/views/meeting-room/hooks/use-meeting-session.ts（修改）
- client/src/views/meeting-room/hooks/use-meeting-stage.ts（新建）
- client/src/views/meeting-room/hooks/use-room-actions.ts（修改）
- client/src/views/meeting-room/types.ts（修改）
- docs/meeting/PRD.md（修改）
- docs/meeting/discussion.md（修改）
- scripts/turn/configure.mjs（修改）
- server/app/services/meeting/ice-configuration.ts（修改）
- server/app/socket/meeting/peer-negotiation-state.ts（修改）
- server/app/socket/meeting/peer-negotiation.ts（修改）
- server/app/socket/meeting/room-state.ts（修改）
- server/app/socket/meeting/schemas.ts（修改）
- server/app/socket/meeting/types.ts（修改）

**Review 结果**: ⚠️ 警告

### 变更摘要

- 聊天草稿、发送锁和结果反馈上移到房间页面，快捷输入与聊天面板共享同一份状态，关闭面板不丢草稿；成功确认只清除未被修改的原草稿，明确失败与结果未确认分开提示，未确认内容需核对记录后显式重发。
- 打开聊天默认定位到最新消息，查看历史时新消息只提示条数不抢滚动位置，历史快照补入的旧消息不计为新到达消息。
- 默认只展示受影响成员及其状态与单独重试入口，连接耗时、重试次数和直连/中继收进可展开诊断；重试入口不再被诊断信息挤占。
- 新增宫格/演示布局与固定成员能力，固定优先于自动跟随共享，共享结束恢复原布局；隐藏自己仅影响本地预览，不关闭设备也不影响其他成员的接收。
- 成员媒体协议增加可选 `isScreenSharing` 布尔值用于呈现，旧数据缺失按未共享处理；服务端协商与 ICE 配置仅做可读性重构，行为与凭证规则不变。
- 抽取宫格、主画面和成员列表共用的 `ParticipantMedia`，避免多次挂载本地视频引用与重复音频出口。
- 将手写源码上限由 200 行放宽到 1500 行，并补充“不压缩语句、不机械拆分”的约束说明，同步更新 agent 规则与 review 清单。

### 验证与边界

- 服务端 `npx tsc --noEmit` 通过，会议相关客户端 ESLint 通过。
- 隔离浏览器验证覆盖双入口草稿同步、迟到确认、未确认重发、历史滚动、成员重试、固定与共享布局切换，手机尺寸检查修复了固定按钮裁切。
- 客户端完整类型检查仍受笔记模块已有的 `UpdateNoteContentVariables` 重复声明阻塞，本批未修改该模块。
- 未验证真实双设备媒体与跨网络 TURN；前后端需一起发布，旧服务端不接受新客户端发送的 `isScreenSharing` 字段。
