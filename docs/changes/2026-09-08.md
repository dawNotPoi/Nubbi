# 变更记录 - 2026-09-08

> 当日统计：1 项待提交 | 275 个变更路径（迁移前后分别统计） | 自动检查通过，真实环境联调未执行

---

## [15:51] commit: `未提交` - refactor(agent): 重构助手三端架构并统一协议与运行边界

**提交者**: dawn

**变更文件**:

- `assistant`：`README.md（修改）`
- `assistant/api`：`package.json（修改）`、`tsconfig.json（修改）`
- `assistant/api/src`：`app.module.ts（修改）`、`main.ts（修改）`、`types.ts（修改）`
- `assistant/api/src/codex`：`account.ts（删除/迁移）`、`client.ts（删除/迁移）`、`dynamic-tools.ts（删除/迁移）`、`protocol.ts（删除/迁移）`、`runner.ts（删除/迁移）`
- `assistant/api/src/common`：`all-exceptions.filter.ts（修改）`、`config-access.guard.ts（修改）`
- `assistant/api/src/config`：`db.ts（修改）`、`env.ts（修改）`
- `assistant/api/src/controllers`：`approvals.controller.ts（删除/迁移）`、`conversations.controller.ts（删除/迁移）`、`extensions.controller.ts（删除/迁移）`、`health.controller.ts（删除/迁移）`、`mcp-config.controller.ts（删除/迁移）`、`model-config.controller.ts（删除/迁移）`、`runs.controller.ts（删除/迁移）`
- `assistant/api/src/lifecycle`：`runtime-lifecycle.service.ts（修改）`
- `assistant/api/src/mcp`：`mcp-config.ts（删除/迁移）`、`mcp.ts（删除/迁移）`
- `assistant/api/src/model`：`model-config.ts（删除/迁移）`、`model.ts（删除/迁移）`
- `assistant/api/src/models`：`conversation.ts（删除/迁移）`、`store.ts（删除/迁移）`
- `assistant/api/src/modules`：`approvals.module.ts（删除/迁移）`、`config.module.ts（删除/迁移）`、`conversations.module.ts（删除/迁移）`、`extensions.module.ts（删除/迁移）`、`health.module.ts（删除/迁移）`、`runs.module.ts（删除/迁移）`
- `assistant/api/src/orchestration`：`agent.ts（删除/迁移）`、`skills.ts（删除/迁移）`、`tool-executor.ts（删除/迁移）`
- `assistant/api/src/runtime`：`approvals.ts（删除/迁移）`、`codex-executor.ts（删除/迁移）`、`context-builder.ts（删除/迁移）`、`event-emitter.ts（修改）`、`events.ts（修改）`、`openai-executor.ts（删除/迁移）`、`provider-executor.ts（删除/迁移）`、`run-event-model.ts（删除/迁移）`、`run-store.ts（删除/迁移）`、`session.ts（删除/迁移）`、`tokens.ts（修改）`、`tool-gateway.ts（删除/迁移）`、`execute-run.ts（新增）`、`executor-factory.ts（新增）`、`message-collector.ts（新增）`、`run-coordinator.ts（新增）`、`run-tools.ts（新增）`
- `assistant/api/src/services`：`approvals.service.ts（删除/迁移）`、`conversations.service.ts（删除/迁移）`、`extensions.service.ts（删除/迁移）`、`mcp-config.service.ts（删除/迁移）`、`model-config.service.ts（删除/迁移）`、`runs.service.ts（删除/迁移）`
- `assistant/mobile`：`App.tsx（修改）`、`package.json（修改）`、`tsconfig.json（修改）`
- `assistant/mobile/src`：`api.ts（修改）`、`storage.ts（删除/迁移）`、`theme.ts（修改）`、`types.ts（修改）`
- `assistant/mobile/src/components`：`controls.tsx（修改）`、`control-styles.ts（新增）`
- `assistant/mobile/src/screens`：`chat-screen.tsx（删除/迁移）`、`connection-screen.tsx（删除/迁移）`、`settings-screen.tsx（删除/迁移）`
- `assistant/mobile/src/screens/chat`：`event-reducer.ts（删除/迁移）`、`history-modal.tsx（删除/迁移）`、`message-view.tsx（删除/迁移）`、`styles.ts（删除/迁移）`、`use-mobile-chat.ts（删除/迁移）`
- `assistant/mobile/src/screens/settings`：`mcp-draft.ts（删除/迁移）`、`mcp-form.tsx（删除/迁移）`、`mcp-panel.tsx（删除/迁移）`、`model-panel.tsx（删除/迁移）`、`model-provider-fields.tsx（删除/迁移）`、`notice.tsx（删除/迁移）`、`styles.ts（删除/迁移）`
- `assistant/web`：`package.json（修改）`、`tsconfig.app.json（修改）`
- `assistant/web/src`：`api-base.ts（删除/迁移）`、`api.ts（删除/迁移）`、`app.tsx（修改）`、`main.tsx（修改）`、`types.ts（修改）`、`use-chat.ts（删除/迁移）`、`use-extensions.ts（删除/迁移）`、`use-model-switcher.ts（删除/迁移）`、`use-slash-command.ts（删除/迁移）`、`vite-env.d.ts（修改）`
- `assistant/web/src/components`：`approval-dialog.tsx（删除/迁移）`、`codex-account-panel.tsx（删除/迁移）`、`composer.tsx（删除/迁移）`、`context-status.tsx（删除/迁移）`、`conversation-drawer.tsx（删除/迁移）`、`extension-list.tsx（删除/迁移）`、`inline-approval.tsx（删除/迁移）`、`key-value-editor.tsx（删除/迁移）`、`mcp-form-utils.ts（删除/迁移）`、`mcp-server-fields.tsx（删除/迁移）`、`mcp-server-form.tsx（删除/迁移）`、`mcp-settings-drawer.tsx（删除/迁移）`、`message-view.tsx（删除/迁移）`、`model-settings-panel.tsx（删除/迁移）`、`model-status-panel.tsx（删除/迁移）`、`model-switcher.tsx（删除/迁移）`、`openai-model-form.tsx（删除/迁移）`、`settings-content.tsx（删除/迁移）`、`slash-palette.tsx（删除/迁移）`、`trace-drawer.tsx（删除/迁移）`
- `assistant/web/src/components/ui`：`button.tsx（修改）`
- `assistant/web/src/lib`：`message-stream.ts（删除/迁移）`、`tokens.ts（删除/迁移）`、`utils.ts（修改）`
- `docs/assistant`：`PRD.md（删除/迁移）`、`pi-agent-runtime-study.md（删除/迁移）`
- `仓库根目录`：`pnpm-lock.yaml（修改）`、`pnpm-workspace.yaml（修改）`
- `assistant/api/src/agent`：`agent-executor.ts（新增）`、`agent-loop.ts（新增）`、`context-builder.ts（新增）`、`run-usage.ts（新增）`
- `assistant/api/src/features/approvals`：`approvals.controller.ts（新增）`、`approvals.module.ts（新增）`、`approvals.service.ts（新增）`
- `assistant/api/src/features/conversations`：`conversation.model.ts（新增）`、`conversation.repository.ts（新增）`、`conversations.controller.ts（新增）`、`conversations.module.ts（新增）`、`conversations.service.ts（新增）`
- `assistant/api/src/features/extensions`：`extensions.controller.ts（新增）`、`extensions.module.ts（新增）`、`extensions.service.ts（新增）`
- `assistant/api/src/features/health`：`health.controller.ts（新增）`、`health.module.ts（新增）`
- `assistant/api/src/features/runs`：`run-event.model.ts（新增）`、`run.repository.ts（新增）`、`runs.controller.ts（新增）`、`runs.module.ts（新增）`、`runs.service.ts（新增）`
- `assistant/api/src/features/settings`：`config.module.ts（新增）`、`mcp-config.controller.ts（新增）`、`mcp-config.service.ts（新增）`、`model-catalog.ts（新增）`、`model-config.controller.ts（新增）`、`model-config.repository.ts（新增）`、`model-config.schema.ts（新增）`、`model-config.service.ts（新增）`
- `assistant/api/src/integrations/codex`：`account.ts（新增）`、`bootstrap.ts（新增）`、`client.ts（新增）`、`dynamic-tools.ts（新增）`、`process-config.ts（新增）`、`protocol.ts（新增）`、`rpc-channel.ts（新增）`、`run-input.ts（新增）`、`runner.ts（新增）`、`thread-lifecycle.ts（新增）`、`turn-identity.ts（新增）`、`turn-stream.ts（新增）`
- `assistant/api/src/integrations/mcp`：`client-pool.ts（新增）`、`mcp-config.ts（新增）`、`mcp.ts（新增）`、`transport.ts（新增）`
- `assistant/api/src/integrations/nubbi`：`approval-review.ts（新增）`
- `assistant/api/src/integrations/skills`：`skill-store.ts（新增）`
- `assistant/api/src/llm/chat-completions`：`adapter.ts（新增）`、`model-discovery.ts（新增）`、`protocol.ts（新增）`、`response.ts（新增）`
- `assistant/api/src/llm`：`model-adapter.ts（新增）`
- `assistant/api/src/tools`：`approvals.ts（新增）`、`registry.ts（新增）`、`scheduler.ts（新增）`、`tool-contracts.ts（新增）`、`tool-executor.ts（新增）`、`validation.ts（新增）`
- `assistant/mobile/src/api`：`connection.ts（新增）`、`conversations.ts（新增）`、`mcp.ts（新增）`、`model.ts（新增）`
- `assistant/mobile/src/features/chat`：`chat-screen.tsx（新增）`、`message-view.tsx（新增）`、`styles.ts（新增）`、`use-mobile-chat.ts（新增）`
- `assistant/mobile/src/features/connection`：`connection-screen.tsx（新增）`
- `assistant/mobile/src/features/conversations`：`history-modal.tsx（新增）`
- `assistant/mobile/src/features/settings`：`mcp-draft.ts（新增）`、`mcp-form.tsx（新增）`、`mcp-panel.tsx（新增）`、`mcp-transport-fields.tsx（新增）`、`model-draft.ts（新增）`、`model-panel.tsx（新增）`、`model-provider-fields.tsx（新增）`、`notice.tsx（新增）`、`settings-screen.tsx（新增）`、`styles.ts（新增）`、`use-codex-account-state.ts（新增）`、`use-codex-account.ts（新增）`、`use-model-settings-state.ts（新增）`、`use-model-settings.ts（新增）`
- `assistant/mobile/src/platform`：`connection-storage.ts（新增）`
- `assistant/shared`：`package.json（新增）`
- `assistant/shared/src/client`：`assistant-client.ts（新增）`、`chat-api.ts（新增）`、`chat-session.ts（新增）`、`chat-state.ts（新增）`、`conversations-api.ts（新增）`、`extensions-api.ts（新增）`、`http-transport.ts（新增）`、`index.ts（新增）`、`mcp-api.ts（新增）`、`message-state.ts（新增）`、`message-stream.ts（新增）`、`model-api.ts（新增）`
- `assistant/shared/src/contracts`：`approvals.ts（新增）`、`events.ts（新增）`、`index.ts（新增）`、`messages.ts（新增）`、`runtime.ts（新增）`、`settings.ts（新增）`
- `assistant/shared/src/streaming`：`sse.ts（新增）`
- `assistant/web/src/features/chat`：`approval-dialog.tsx（新增）`、`composer.tsx（新增）`、`context-status.tsx（新增）`、`inline-approval.tsx（新增）`、`message-view.tsx（新增）`、`token-estimate.ts（新增）`、`use-chat.ts（新增）`
- `assistant/web/src/features/commands`：`slash-commands.ts（新增）`、`slash-palette.tsx（新增）`、`use-slash-command-state.ts（新增）`、`use-slash-command.ts（新增）`
- `assistant/web/src/features/conversations`：`conversation-drawer.tsx（新增）`
- `assistant/web/src/features/extensions`：`extension-list.tsx（新增）`、`use-extensions-state.ts（新增）`、`use-extensions.ts（新增）`
- `assistant/web/src/features/models`：`model-status-panel.tsx（新增）`、`model-switcher.tsx（新增）`、`use-model-switcher-state.ts（新增）`、`use-model-switcher.ts（新增）`
- `assistant/web/src/features/settings`：`codex-account-panel.tsx（新增）`、`key-value-editor.tsx（新增）`、`mcp-form-utils.ts（新增）`、`mcp-server-fields.tsx（新增）`、`mcp-server-form.tsx（新增）`、`mcp-settings-drawer.tsx（新增）`、`model-settings-panel.tsx（新增）`、`openai-model-form.tsx（新增）`、`settings-content.tsx（新增）`、`use-assistant-settings-state.ts（新增）`、`use-assistant-settings.ts（新增）`、`use-codex-account.ts（新增）`、`use-model-settings-state.ts（新增）`、`use-model-settings.ts（新增）`
- `assistant/web/src/features/trace`：`trace-content-node.tsx（新增）`、`trace-drawer.tsx（新增）`、`trace-events.ts（新增）`、`trace-format.ts（新增）`、`trace-lifecycle-node.tsx（新增）`、`trace-node.tsx（新增）`、`trace-timeline.tsx（新增）`、`trace-tool-node.tsx（新增）`、`trace-types.ts（新增）`、`use-trace-playback-state.ts（新增）`、`use-trace-playback.ts（新增）`
- `assistant/web/src/platform`：`assistant-api.ts（新增）`、`config-access-storage.ts（新增）`

**Review 结果**: ⚠️ 自动检查与模拟回归通过；真实模型、MCP 调用、Codex 登录续接和手机现场交互未验证。

### 变更摘要

- 后端按业务模块、Runtime、Agent、LLM、Tools 和 Integrations 划分职责；区分单轮 ModelAdapter 与完整任务 AgentExecutor，连接配置在组装阶段注入。
- 增加私有 workspace 包，统一三端消息、配置、审批及运行事件协议，复用 SSE 解析、HTTP 请求及聊天业务状态，保留平台传输、存储与界面。
- Web、Mobile 按功能归组，拆分聊天、模型设置、账号登录、MCP 表单及运行轨迹；补充显式类型和中文 JSDoc，手写源码文件保持不超过 200 行。
- 修正非法工具参数、异常断流、审批取消、队列取消、旧请求串线、Codex 轮次隔离、工具结果匹配、部分输出保存和 token 用量重复累计。
- 保留只读工具最多四路并发、写工具串行及原审批策略；不自动重试写操作，不新增模型协议。
- 更新 README 的 MCP HTTP/stdio、自有 Loop 与代码阅读顺序；保留此前已授权删除的两份旧 Assistant 文档，不恢复过时材料。
- 保留既有技术栈、外部 JSON 字段、启动命令及 TypeScript 源文件导入方式；不修改历史数据库、真实配置或凭据。

### 验证记录

- `pnpm build:assistant` 通过：Web 生产构建、API 与 Mobile 类型检查。
- `pnpm --filter nubbi-assistant-web lint` 通过，0 错误、0 警告。
- Expo Android 离线导出通过，共享包可由 Metro 解析。
- 仓库外临时脚本完成 29 项模拟/只读验证，包括流式分片、多轮工具、Skill 激活、审批允许/拒绝、取消重发、Codex 通知与续接回退；未调用真实模型或写工具。
- 176 个 TypeScript 源文件的相对依赖检查未发现循环，公开导出已补文档，主循环不包含厂商协议字段，公共包不依赖平台框架。
- `git diff --check` 通过；未新增仓库测试文件。
