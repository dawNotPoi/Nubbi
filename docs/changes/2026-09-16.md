# 2026-09-16 变更记录

## UI 重构 WIP：补充 dev/out PR 验证

**提交者**: dawn（Agent 协助；用户已授权在同一个 WIP PR 持续提交）
**变更文件**:
- `.github/workflows/ui-review.yml`（新增）

**Review 结果**: ⚠️ 静态审查完成，实际 Actions 运行待确认。

### 变更摘要
- 为目标为 `dev/out` 的客户端 PR 增加独立的依赖、锁文件、lint、类型和构建检查，不触发正式部署。
- 验证工作流只读仓库，不使用生产凭据，不自动提交或推送。安装产生的锁文件差异单独报错，避免把未锁定的依赖标成通过。
- 短期保留源码快照与构建产物，支持本地网络不可用时复核本次源码和真实 pnpm 锁文件。
- 本轮沿用 `refactor/ui-theme-v1` 与 Draft PR #5；不合并 `dev/out` 或 `deploy`。


## UI 重构 WIP：升级 Tailwind 4.3.3 并迁入 Base UI

**提交者**: dawn（Agent 协助；用户明确授权同一个 WIP PR 持续 commit）

**Review 结果**: ⚠️ 已做源码和语法检查，完整依赖、lint、类型、构建及浏览器效果以独立验证结果为准。

### 变更摘要
- 固定客户端 Tailwind 与 Vite 插件到 npm 当前稳定版 4.3.3；移除旧 PostCSS/Autoprefixer/滚动条插件接入，保留 Vite 5 和其他工作区。
- 补齐 CSS 层顺序、旧语义映射、编辑器 @reference、侧栏位移动效与 v3 外观兼容。
- 安装固定版本 Base UI 1.8.0；迁移 Button、Input、Checkbox、DropdownMenu 以及 NoteLibrary 对应调用，保留复杂 Select 与既有确认弹层。
- AntD 颜色从 theme.css 派生；页面结构、列宽、路由、接口、笔记控制器不变。
- 通过一次性、仅限当前 WIP 分支的 CI 任务应用经过 SHA 校验的补丁并生成真实锁文件；任务比对远端 SHA，拒绝覆盖更新，不部署、不合并，提交后移除自身引导文件。
- 仍为 Draft PR #5；预览链接、真实设备交互及主按钮文字对比度仍是未完成验收项。

**变更文件**:
- `.agent/module-index.md`
- `.agent/skills/review.md`
- `.agent/skills/style.md`
- `AGENTS.md`
- `client/package.json`
- `client/postcss.config.js`
- `client/src/AppProvider.tsx`
- `client/src/Route.tsx`
- `client/src/component/SideBar/ResizeTab/index.tsx`
- `client/src/component/editor/Tiptap/index.css`
- `client/src/components/ui/button.tsx`
- `client/src/components/ui/checkbox.tsx`
- `client/src/components/ui/dropdown-menu.tsx`
- `client/src/components/ui/input.tsx`
- `client/src/features/note/components/MarkdownImportButton.tsx`
- `client/src/features/note/components/NoteLibraryBatchActionBar.tsx`
- `client/src/features/note/components/NoteLibraryOptionMenu.tsx`
- `client/src/features/note/components/NoteLibraryRow.tsx`
- `client/src/features/note/components/NoteLibraryTable.tsx`
- `client/src/features/note/components/NoteLibraryToolbar.tsx`
- `client/src/index.css`
- `client/src/styles/antd-theme.ts`
- `client/src/styles/primitives.css`
- `client/src/styles/tailwind-compat.css`
- `client/src/theme.css`
- `client/src/views/NoteLibrary.tsx`
- `client/tailwind.config.js`
- `client/vite.config.ts`
- `docs/infrastructure/PRD.md`

### 锁文件同步
- 一次性 CI 已通过 pnpm 解析真实依赖并同步 pnpm-lock.yaml；安装及项目验证在后续只读任务中独立执行。

## UI WIP：恢复持续冻结安装验证

**变更文件**：`.github/workflows/ui-review.yml`（修改）。

**Review 结果**：已确认升级源码与真实锁文件提交于 `0a103ea4d7adcc8b6fa306007227aeb256c346e7`；一次性写入工作流和补丁已删除。

- 持续 PR 验证改为 `pnpm install --frozen-lockfile`，只读权限，不安装时更新锁文件、不自动提交。
- 本提交只收紧 CI，不再次变更业务代码；每项验证结论以对应提交的 Actions 结果为准。
- 保留 PR #5 的 Draft 状态；不合并、不触发生产部署。
