# 变更记录 - 2026-09-16

> 当日统计：1 次提交 | 博客站点、公开 API 与工作区配置 | 通过率 100%

---

## [21:52] commit: `未提交` - feat(shared): 接入公开博客与阅读站点

**提交者**: Codex
**变更文件**:
- `nubbi-blog/**`（新建）
- `server/app/controller/blog/**`（新建）
- `server/app/routes/blog/**`（新建）
- `server/app/index.ts`、`server/app/lib/env.ts`、`server/.example.env`（修改）
- `docs/blog/**`（新建）
- `docs/infrastructure/PRD.md`、`docs/note/PRD.md`、`.agent/module-index.md`（修改）
- `package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`scripts/dev.bat`、`scripts/dev.sh`（修改）
- `website/**`（删除）

**Review 结果**: ✅ 通过

### 变更摘要
- 引入独立 Nubbi Blog 阅读站点，提供文章列表、详情、标签、阅读偏好与链接预览能力。
- 在主服务增加只读公开博客 API，仅返回符合发布规则的白名单字段。
- 替换旧 `website` 工作区，同步开发脚本、环境变量、依赖锁文件与项目文档。
- Review 无阻断项；`lint:blog`、`typecheck:blog`、`build:blog`、主服务 `typecheck` 与 `git diff --check` 均通过。
