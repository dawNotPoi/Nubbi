# 变更记录 - 2026-08-14

> 当日统计：1 次提交 | 7 文件变更 | 通过率 100%

---

## [17:10] commit: `未提交` - feat(agent): 对话工具调用链路展示与耗时持久化

**提交者**: agent
**变更文件**:
- docs/assistant/PRD.md（修改，新增工具调用链路展示章节与 durationMs 约定）
- assistant/api/src/runtime/events.ts（修改，tool-result 事件增加 durationMs 字段）
- assistant/api/src/runtime/tool-gateway.ts（修改，计算工具执行耗时并写入事件与 tool part）
- assistant/api/src/types.ts（修改，tool part 增加 durationMs 字段）
- assistant/web/src/components/message-view.tsx（修改，ToolCard 重写为状态徽标+参数/结果折叠+耗时）
- assistant/web/src/lib/message-stream.ts（修改，tool-start 写入 callId；tool-result 按 callId 匹配并写入 success/durationMs）
- assistant/web/src/types.ts（修改，part 与 StreamEvent 增加 durationMs）

**Review 结果**: ✅ 通过

### 变更摘要
- Web 端对话内以 DeepSeek Harness 风格展示工具调用链路：状态标记（运行中/成功/失败）、可折叠参数与结果、耗时显示。
- 服务端在工具实际执行阶段计时（不含审批等待），durationMs 随 tool-result 事件与 tool part 持久化，历史消息缺失该字段时前端不展示。
- 修复并发工具调用错配：tool-start 写入 callId，tool-result 优先按 callId 匹配，旧事件无 callId 时回退 server+tool 匹配。
- 持久化与对话绑定无需新接口：tool part 随 conversation 文档落库，历史加载完整还原；callId 贯穿事件与 part，为未来按 Run 回放链路预留扩展点。

## [18:05] commit: `未提交` - feat(agent): 会话缓存命中率查询工具

**提交者**: agent
**变更文件**:
- assistant/api/src/model/model.ts（修改，解析 usage 的 prompt_cache_hit/miss_tokens）
- assistant/api/src/types.ts（修改，TokenUsage 增加缓存字段）
- assistant/api/src/runtime/events.ts（修改，token-usage 事件携带缓存字段）
- assistant/api/src/models/conversation.ts、store.ts（修改，tokenUsage 持久化缓存累计）
- assistant/api/src/runtime/tool-gateway.ts（修改，新增内置只读工具 assistant_session_cache_stats）
- assistant/api/src/runtime/provider-executor.ts、session.ts（修改，注入 runUsage 与会话统计读取）
- assistant/api/src/orchestration/agent.ts（修改，注册内置工具并累计缓存 token）
- assistant/web/src/types.ts（修改，TokenUsage/事件类型同步）
- docs/assistant/PRD.md（修改，补充会话缓存命中率查询说明）

**Review 结果**: ✅ 通过

### 变更摘要
- 解析模型 API（OpenAI 兼容）usage 中的 prompt 缓存统计字段，按对话累计持久化到 tokenUsage。
- 新增内置只读工具 `assistant_session_cache_stats`：对话中问助手即可返回当前会话缓存命中率，结果为 { hit_tokens, miss_tokens, hit_rate }，hit_rate 为 0~1 浮点数；无缓存数据时返回 0。
- 命中率 = 命中 token / (命中 + 未命中)，数据含对话历史累计与当前 Run 实时累计。

## [18:40] commit: `未提交` - feat(mcp): 默认公开 Host 校验与 0.0.0.0 绑定

**提交者**: agent
**变更文件**:
- docker-compose.yml（修改，MCP_ALLOWED_HOSTS 默认留空、MCP_BIND_ADDRESS 默认 0.0.0.0）
- mcp/src/config.ts（修改，移除默认 Host 白名单回退，空 = 不校验）
- mcp/src/transport/http.ts（修改，allowedHosts 非空才启用 hostHeaderValidation）
- mcp/src/types.ts（修改，注释说明 allowedHosts 空数组语义）
- mcp/.env.example、mcp/README.md（修改，示例同步新语义）

**Review 结果**: ✅ 通过

### 变更摘要
- MCP_ALLOWED_HOSTS 留空时不再回退 localhost 白名单，而是完全不校验 Host 头（默认公开），便于开发测试直接远程访问；显式填写时白名单拦截照常生效（已实测：白名单实例对伪造 Host 返回 403，默认公开实例放行后由 Bearer token 鉴权兜底）。
- docker-compose 端口映射默认绑定 0.0.0.0（原 127.0.0.1），生产环境可在根 .env 设置 MCP_BIND_ADDRESS=127.0.0.1 收紧。

## [11:00] commit: `未提交` - chore(ci): 部署不再包含 mcp 容器

**提交者**: agent
**变更文件**:
- scripts/deploy-docker.sh（修改，stop/rm/up 移除 mcp 容器）

**Review 结果**: ✅ 通过

### 变更摘要
- 服务器内存有限（2H2G），mcp 容器不再由 CI 部署：assistant 已支持本地 stdio 连接 mcp，无需服务器常驻 mcp 容器。
- 部署脚本 stop/rm 列表去掉 mcp，up 时 --remove-orphans 自动清理服务器上已存在的 mcp 容器；需要时仍可手动 docker compose up -d mcp。

## [11:10] commit: `未提交` - feat(agent): MCP 支持 stdio 本地传输

**提交者**: agent
**变更文件**:
- assistant/api/src/mcp/mcp-config.ts（修改，schema 支持 transport: http/stdio）
- assistant/api/src/mcp/mcp.ts（修改，createTransport 支持 StdioClientTransport）
- assistant/api/src/services/extensions.service.ts（修改，transport 联合类型、endpoint 兼容 stdio）
- assistant/web/src/types.ts、components/settings-content.tsx（修改，类型与列表展示支持 stdio）
- assistant/mobile/src/types.ts、screens/settings/mcp-draft.ts（修改，类型兼容 stdio）

**Review 结果**: ✅ 通过

### 变更摘要
- assistant 的 MCP 配置支持 stdio 传输（command/args/env），本地直接拉起 nubbi-mcp-server 子进程，替代服务器常驻的 HTTP mcp 容器，减轻 2H2G 服务器负担。
- 配置示例：nubbi server 改为 transport: stdio + command node + env NUBBI_API_URL/NUBBI_API_KEY（支持 ${ENV} 展开）。
- HTTP 模式完全保留；三个包类型检查通过。

## [14:30] commit: `未提交` - feat(agent): MCP 表单支持双传输与工具清单展示

**提交者**: agent
**变更文件**:
- assistant/api/src/mcp/mcp-config.ts（修改，ID 自动生成、cwd 可选字段、id 可省略）
- assistant/api/src/mcp/mcp.ts（修改，stdio 传输支持 cwd）
- assistant/api/src/services/extensions.service.ts（修改，id 可选）
- assistant/web/src/components/mcp-form-utils.ts（修改，Draft 支持双传输与 cwd，ID 由后端生成）
- assistant/web/src/components/mcp-server-fields.tsx（新建，HTTP/STDIO 字段区组件）
- assistant/web/src/components/mcp-server-form.tsx（修改，传输切换、去 ID 输入、测试工具清单展示）
- assistant/web/src/components/mcp-settings-drawer.tsx（修改，test 返回结构化结果）
- assistant/web/src/components/settings-content.tsx、types.ts（修改，id 可选、STDIO 徽标）
- assistant/mobile/src/screens/settings/mcp-draft.ts、mcp-form.tsx、mcp-panel.tsx、styles.ts（修改，传输切换、cwd、工具清单展示）
- assistant/mobile/src/api.ts、types.ts（修改，id 可选）

**Review 结果**: ✅ 通过

### 变更摘要
- 设置表单支持 HTTP/STDIO 双传输切换：stdio 模式填写启动命令、参数、可选工作目录、环境变量（参考 Codex mcp_servers 配置形态）。
- MCP ID 不再由前端填写：新增时只填名称，后端按名称 slug 自动生成（冲突追加随机后缀），编辑时 ID 只读展示。
- 测试连接成功时展示发现工具清单（名称 + 描述），Web 与 Mobile 同步。
- 全链路验证：stdio 发现 nubbi 12 个工具，测试连接接口返回完整工具清单。
