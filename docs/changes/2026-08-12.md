# 变更记录 - 2026-08-12

> 当日统计：1 次提交 | 0 文件变更 | 通过率 0%

---

## [未填] commit: `未提交` - feat(agent): 新增模型/技能/MCP管理与上下文token状态面板

**提交者**: dawn
**变更文件**:
- `assistant/api/src/runtime/tokens.ts`（新建：CJK/ASCII token 估算）
- `assistant/api/src/runtime/context-builder.ts`（改为 token 预算 + 返回占用信息）
- `assistant/api/src/runtime/events.ts`（新增 context-status / token-usage 事件）
- `assistant/api/src/runtime/session.ts`（推送上下文状态、token 用量并落库）
- `assistant/api/src/runtime/provider-executor.ts`（执行器返回 usage）
- `assistant/api/src/model/model.ts`（请求带 include_usage 解析 usage；非 JSON 响应可读报错）
- `assistant/api/src/model/model-config.ts`（新增 contextWindow 字段）
- `assistant/api/src/orchestration/agent.ts`（多轮累计 token 用量）
- `assistant/api/src/orchestration/skills.ts`（技能启用状态存储与过滤）
- `assistant/api/src/models/conversation.ts`、`store.ts`（对话持久化 tokenUsage）
- `assistant/api/src/controllers/extensions.controller.ts`、`mcp-config.controller.ts`（技能/服务启停接口）
- `assistant/api/src/controllers/conversations.controller.ts`（拒绝含 U+FFFD 乱码消息）
- `assistant/api/src/services/extensions.service.ts`、`mcp-config.service.ts`（启停逻辑）
- `assistant/api/src/main.ts`（容忍空 JSON body）
- `assistant/api/src/codex/runner.ts`（适配执行器新返回）
- `assistant/api/src/config/env.ts`（projectRoot 修正）
- `assistant/api/src/runtime/openai-executor.ts`（注释更新）
- `assistant/mobile/src/api.ts`、`assistant/web/src/api.ts`（空 body 不设 JSON content-type）
- `assistant/web/src/components/`（model-switcher、slash-palette、model-status-panel、extension-list、context-status、openai-model-form、composer 等）
- `assistant/web/src/use-model-switcher.ts`、`use-slash-command.ts`、`use-extensions.ts`、`use-chat.ts`、`app.tsx`
- `assistant/web/src/lib/tokens.ts`、`message-stream.ts`（token 估算与 SSE 事件折叠）
- `assistant/web/src/types.ts`（新增 TokenUsage、context-status、token-usage 事件）
- `.gitignore`（忽略 assistant/config/skills.json）

**Review 结果**: ✅ 通过

### 变更摘要
- 模型与能力管理：新增输入框上方模型选择器与 `/model`、`/skill`、`/mcp`、`/status` 斜杠命令；技能新增启用状态（config/skills.json），技能/MCP 均可在命令面板行内启停。
- 上下文与 token 统计：模型请求带 `stream_options.include_usage` 解析真实 usage，按对话累计持久化；上下文改为 token 预算自动压缩；新增 `/status` 面板展示模型、订阅计划、token 用量、上下文占用与能力状态。
- 鲁棒性修复：空 body 的 POST/DELETE 不再设 JSON content-type，服务端容忍空 JSON body；模型非 JSON 响应给出可读报错；拒绝含 U+FFFD 的乱码消息，避免脏数据入库。
- 全端注释遵循中文 JSDoc 规范，文件均 ≤ 200 行。
