# 变更记录 - 2026-08-07

> 当日统计：1 次提交 | 8 文件变更 | 通过率 100%

---

## [00:00] commit: `未提交` - refactor(server): 升级 Express 至 5.2.1 并重构异步错误处理

**变更文件**:
- server/package.json（修改）
- server/app/index.ts（修改）
- server/app/middleware/common.ts（修改）
- server/app/routes/file/access.ts（修改）
- server/app/routes/file/upload.ts（修改）
- server/app/routes/image/index.ts（修改）
- server/app/routes/infrastructure/json-route-registrar.ts（修改）
- pnpm-lock.yaml（修改）

**Review 结果**: ✅ 通过

### 变更摘要
- 升级 Express 4.19.2 → 5.2.1
- 移除 body-parser 依赖，改用 Express 5 内置 express.json()/urlencoded()
- 适配 path-to-regexp v8 路径语法：`*` 通配符 → `{:.*}`
- 删除冗余 `app.options("*")`（cors 中间件已覆盖）
- `asyncHandler` 重构为 `withAccountContext`：移除手动 `.catch(next)`，利用 Express 5 原生异步错误捕获，保留 ALS 上下文与变更锁释放
- `pnpm typecheck` 零错误通过
