# 变更记录 - 2026-09-22

> 当日统计：2 次提交 | 136 文件变更 | 通过率 100%

---

## [14:56] commit: `未提交` - refactor(client): 移除 AntD 并统一共享 UI 源码层

**提交者**: Agent
**变更文件**:
- client/package.json（修改：删除 antd、@ant-design/icons、@ant-design/cssinjs）
- pnpm-lock.yaml（修改：同步移除 AntD 相关依赖，减少 893 行）
- client/components.json（新建：手动接入 shadcn Base UI 配置，未运行 init）
- client/public/oauth-callback.html（新建：仅通知 OAuth 流程结果）
- client/src/components/ui/**（新建 18 个共享控件：alert、badge、confirm-dialog、context-menu、dialog、password-input、popover、progress、select、skeleton、spinner、switch、tabs、textarea、toast、tooltip 等）
- client/src/components/ui/{button,input,sheet}.tsx（修改：统一尺寸、焦点与转导出规则）
- client/src/component/UI/**（修改：旧路径降为纯转导出，不再保留重复实现）
- client/src/AppProvider.tsx（修改：移除 AntD 注入，挂载 ConfirmDialogHost 与 Toaster）
- client/src/main.tsx、client/src/index.css、client/src/styles/**（修改：移除 `@layer antd` 与 `.ant-*` 覆盖）
- client/src/styles/antd-theme.ts、client/src/styles/auth.css（删除）
- client/src/features/auth/model/oauth-popup.ts（新建：同源 BroadcastChannel 隔离的登录窗口）
- client/src/features/auth/model/{session-coordinator,types}.ts（修改：OAuth 窗口流程与退出语义）
- client/src/views/login/{index.tsx,AuthShell.tsx,SocialLoginButtons.tsx,auth-shell.css}、auth-error.ts（修改或新建：登录页迁移与错误中文化）
- client/src/views/reset-password/index.tsx（修改：迁移到共享控件）
- client/src/component/**、client/src/features/**、client/src/views/**（修改：会议、文件、笔记、回收站、编辑器、上传、账户、Token 等页面迁移到共享控件）

**Review 结果**: ⚠️ 静态 review 与构建验证通过；静态 UI review 覆盖危险确认、账号作用域、Portal 冒泡、异步提交、移动端 Sheet、焦点与触控目标，无阻塞项；6 项延后问题已登记在 docs/ui/rebuild-plan.md 第 10 节，未做真实浏览器端到端验收

### 变更摘要

- 主客户端移除 Ant Design 运行依赖、静态消息、图标与 CSS-in-JS Provider，改为 `components/ui` 项目自有 shadcn 风格源码层，以 Base UI 为交互底座；不重新执行 `shadcn init`，不新增第三方组件库。
- 补齐 Dialog/Confirm/Select/Tabs/Tooltip/Toast 与状态类基础组件；`Modal` 桌面用 Dialog、移动用共享 Sheet，危险操作用 AlertDialog，异步提交阻止重复确认且失败保留内容。
- 修复迁移引入的业务回归：创建会议的开始时间恢复旧 `DatePicker minDate` 约束，改为 `min`（当前时间 − 5 分钟）+ 提交前校验，两层共用同一宽限值，避免拦掉弹窗打开时的默认值。
- GitHub 桌面登录改用独立窗口，移动端或弹窗被拦截时沿用整页跳转；回调页只广播流程结果，不传用户或 token；取消授权静默返回，失败与超时使用 toast。
- 退出改为立即清空本地身份并回到登录页；远端确认失败发布 `signOutFailed`，登录页提供持久通知与「重试退出」，退出意图写入当前标签页并在刷新后继续退出，远端确认前禁止新登录。
- 删除确认框跨账号销毁、上传与文件批量操作、编辑器与会议交互等业务语义未改变；未新增或修改任何测试文件。
- 验证：`pnpm exec tsc -b`、`pnpm lint`（0 error，保留 `utils/common.ts` 原有 4 条 any 警告）、`pnpm build`（仅主包体积警告）、`git diff --check` 全部通过；另抽取源码 305 个任意值工具类与 34 个语义 Token 类名与构建产物 CSS 逐一比对，未发现引用不存在 Token 的死类名。

---

## [14:56] commit: `未提交` - docs(agent): 统一方案口径并登记 UI 遗留项

**提交者**: Agent
**变更文件**:
- docs/ui/antd-removal-plan.md、docs/ui/antd-removal-verification.md（新建：迁移计划、组件契约与两轮验收记录）
- docs/ui/rebuild-plan.md（修改：新增第 10 节已知遗留与待办、更新阶段说明）
- docs/ui/discussion.md（修改：新增第 2 轮决策记录）
- docs/meeting/PRD.md（修改：创建会议表单契约与时间约束）
- docs/auth/PRD.md（修改：OAuth 取消静默、登录错误提示口径、组件表路径）
- docs/auth/{auth-session-isolation-design.md,auth-session-isolation-implementation-plan.md,auth-refactor-verification.md,resource-isolation-matrix.md,better-auth-upgrade-plan.md,discussion.md,login-flow-bugs.md}（修改：退出语义对齐现行口径、历史记录加标注、修正失效路径）
- docs/note/{PRD.md,refactor-plan.md}、docs/knowledge-base/phase{1,3,4}-*.md（修改：修正失效路径与组件名，计划阶段加状态标注）
- docs/infrastructure/PRD.md（修改：技术栈与共享组件规则改为 AntD 已移除的口径）
- .agent/rules/core.md（修改：新增「方案优先（单一事实来源）」）
- .agent/skills/{workflow.md,review.md,style.md}、.agent/module-index.md（修改：同步方案优先、技术栈与路径口径）
- AGENTS.md（修改：技术栈表移除 Ant Design，工作流补方案唯一来源）
- .gitignore（修改：补 `node_modules/`、`dist/`、`.next/`、`*.tsbuildinfo`、`.superpowers/` 兜底）

**Review 结果**: ✅ 通过；方案文档之间不再存在旧口径，路径引用校验无失效项（计划中的 knowledge-base 阶段已加状态标注）

### 变更摘要

- 按用户要求把“方案文档作为需求唯一来源”写入规则：`.agent/rules/core.md` 新增「方案优先」，`AGENTS.md` 与 `.agent/skills/workflow.md` 同步；先改方案、再改代码、同一 commit 保持一致。
- 三项决策落到模块方案：会议时间约束进 `docs/meeting/PRD.md`；OAuth 取消静默与登录错误提示进 `docs/auth/PRD.md`；`Modal` 的 `onCancel` 旧契约进 `docs/ui/antd-removal-plan.md`。
- 暂不处理的 7 项（浮层层级坐标、移动端触控目标、`toast as message` 过渡命名、Popover 回调语义、OAuth 超时强关窗口、Tabs 函数式 className、Divider/Image 未收拢）集中登记在 `docs/ui/rebuild-plan.md` 第 10 节，不在聊天记录里留待办。
- 清理方案冲突：删除“AntD 仍处迁移期兼容层”“`component/UI` 是旧兼容区”“OAuth 错误留在卡片内”“远端成功才发布 anonymous”等旧口径；`website/` 相关约束与描述一并移除（该目录已删除）。
- 修正规范文档中已失效的源码路径与组件名（`NoteCard`、`noteAtom`、`controller/note/query.ts`、`common/chalk.ts`、`component/auth` 等），并给 phase1/3/4 计划文档加状态标注，避免把待创建文件误读为现状。
- `.gitignore` 补齐兜底规则：`node_modules/`（原 `/node_modules` 只覆盖根目录）、`dist/`、`.next/`、`*.tsbuildinfo`、`.superpowers/`；同时删除 `website/` 残留构建产物 6.8M。

---

## [15:30] commit: `未提交` - fix(blog): 修复 React 类型解析并完成接口对接

**提交者**: Agent
**变更文件**:
- package.json（修改：根 devDependencies 固定 `@types/react@19.2.17`）
- nubbi-blog/package.json（修改：`@types/react` 19.2.18 → 19.2.17，与工作区统一）
- pnpm-lock.yaml（修改：同步包解析结果）
- nubbi-blog/.env.example（修改：说明 `NUBBI_API_URL` 是 API 基地址及部署前提）
- docs/blog/PRD.md（修改：新增「接口对接与运行（2026-09-22 实测）」）
- docs/infrastructure/dependency-management.md（修改：子项目数改为四个，新增工作区级 `@types/react` 说明）

**Review 结果**: ✅ 通过；blog 的 typecheck / lint / build 全部恢复并通过，接口契约与渲染链路实测一致

### 变更摘要

- 定位并修复 blog 长期无法 typecheck/build 的问题：Next.js `dist/client/link.d.ts` 的 `import React from 'react'` 从 `node_modules/.pnpm/next@…/` 向上查找 `@types/react` 时命不中 pnpm 虚拟 store，`React` 退化为 `any`，`Link` 的 props 失去类型并报 `TS7006`；`skipLibCheck` 隐藏了库内错误，因此只看到业务侧的隐式 any。
- 修复方式：根 `package.json` 固定 `@types/react@19.2.17` 让仓库根 `node_modules/@types/react` 可见；`nubbi-blog` 同步固定同一版本，消除 19.2.17 / 19.2.18 两份 React 类型实例（此前报 `Two different types with this name exist`）。已在依赖管理文档写明"不要删、不要拆成两个版本"。
- 接口对接实测（本地 server 连测试库 `Nubbi`）：`/blog/posts` 的分页字段、`order` 非法值 400、`limit > 50` 拒绝、`/blog/tags` 结构、详情 404 行为全部与 `docs/blog/PRD.md` 契约一致；中文 `q` / `tag` 需百分号编码（客户端 `URLSearchParams` 已满足）。库里 34 篇笔记当前 `published` 全为 false，列表返回空结果是正确行为。
- 渲染链路用同契约响应验证：列表（标题/摘要/标签/日期/总数）、标签云、详情页 Markdown（`h1`/`h2`/列表/行内代码/代码块）均正确渲染，dev 日志无 `BlogApiError`；对真实 server 的首页返回 200 并渲染空态。
- 部署前提已写入方案：主服务必须是包含 `/blog` 只读路由的版本。线上 `dawnpoi.site:4000` 目前仍是旧构建（`/blog/*` 返回 404、`/tag/list` 返回 401），需要先部署本模块的主服务；站点根域由 nginx 做 SPA 回退，不能作为 API 地址。
- 验证：`pnpm --filter nubbi-blog typecheck` / `lint` / `build`（5 条路由，静态页面生成正常）通过；`client` `tsc -b` 与 `server` `typecheck` 同步回归通过。未新增或修改测试文件，未写入测试库任何数据（仅只读查询与索引同步）。
