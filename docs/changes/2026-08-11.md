# 变更记录 - 2026-08-11

> 当日统计：2 次提交 | 通过率 100%

---

## [09:45] commit: `未提交` - docs(agent): 补充 Assistant 全端中文代码注释

**提交者**: dawn
**变更文件**:
- `assistant/api/src/**`（为 Runtime、Codex 客户端、审批、配置与编排链路补充中文注释）
- `assistant/web/src/**` 及配置（为 UI 组件、聊天状态与工具表单补充中文注释）
- `assistant/mobile/src/**` 及配置（为移动端聊天、设置与存储逻辑补充中文注释）

**Review 结果**: ✅ 通过

### 变更摘要
- 为 Assistant 的 API（服务端）、Web 与移动端核心逻辑补充中文注释，覆盖 Codex JSON-RPC 客户端、ToolGateway 执行调度、审批超时、上下文预算截断与事件分发等非显而易见路径。
- 将若干常量改为带业务说明的注释，统一既有注释为中文 JSDoc 形式，不改变运行行为。
- 仅补充注释与文档说明，无逻辑改动。

## [13:15] commit: `未提交` - feat(agent): 模型配置支持自定义请求头与温度并重构编排为手写循环

**提交者**: dawn
**变更文件**:
- `assistant/api/src/agent.ts`（编排核心：LangGraph 图改为手写循环）
- `assistant/api/src/orchestration/agent-graph.ts`、`agent-state.ts`（删除，随依赖移除）
- `assistant/api/src/model-config.ts`、`model.ts`（新增自定义请求头与可选温度）
- `assistant/api/src/model-config.service.ts`、`controllers/model-config.controller.ts`（同步新配置字段）
- `assistant/api/src/**`、`assistant/web/src/**`、`assistant/mobile/src/**`（全端函数补全 JSDoc @param/@returns）
- `assistant/web/src/types.ts`、`assistant/mobile/src/types.ts`（ModelConfig 新增 headers/temperature）
- `assistant/web/src/components/model-settings-panel.tsx`、`assistant/mobile/src/screens/settings/model-panel.tsx`（设置页新增请求头编辑器与温度输入）
- `assistant/api/package.json`、`pnpm-lock.yaml`（移除 @langchain/core 与 @langchain/langgraph）
- `AGENTS.md`、`.agent/rules/core.md`（新增函数 JSDoc 注释规范）

**Review 结果**: ✅ 通过

### 变更摘要
- 编排重构：将 LangGraph StateGraph 编排替换为与 pi 类似的代码原生手写循环（model ↔ tools ↔ 轮数上限），删除 agent-graph/agent-state 与 langchain 依赖，行为等价。
- 模型配置：支持自定义请求头（可覆盖默认 Content-Type/Authorization，便于接第三方中转）与可选采样温度，服务端/Web/移动端全链路打通。
- 注释规范：全端函数补全 JSDoc @param/@returns，并将该规范写入 AGENTS.md 与 .agent/rules/core.md，供后续 agent 遵循。
