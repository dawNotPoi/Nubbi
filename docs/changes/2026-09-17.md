# 2026-09-17 变更记录

## 品牌资产准备

**提交者**: Agent
**变更文件**: .github/workflows/import-brand-assets.yml、.github/scripts/import-brand-assets.py、.github/scripts/brand-import-manifest.json、client/public/brand/favicon.png
**Review 结果**: 静态审查通过；导入工作流仅运行于隔离分支，不部署、不修改认证、不访问生产密钥；成品哈希校验失败即终止。
**变更摘要**: 从已确认源图裁切并压缩品牌图片，通过一次性校验导入写入真实图片文件。导入完成后删除临时工作流与脚本；正式页面不依赖分块文件。

## 品牌资源导入

**提交者**: github-actions[bot]
**变更文件**: client/public/brand/{mascot,wordmark,auth-scene}.webp
**Review 结果**: 分块 Git SHA、成品 SHA256、字节数与 WebP 容器校验通过；本地已检查压缩图像。
**变更摘要**: 使用用户确认的高光眼便签精灵与登录插画，不重绘 Logo，不修改认证业务。

## UI 审查构建准备

**提交者**: Agent
**变更文件**: 删除一次性品牌导入工作流、脚本与清单；新增 .github/workflows/ui-review.yml、docs/ui/brand-assets.md。
**Review 结果**: 静态审查通过；审查构建只读仓库、不部署、不连接生产服务，产物保留三天。
**变更摘要**: 为真实前端 bundle 的桌面/移动渲染审查提供可下载构建产物。图片已导入，不再依赖临时导入工具。
