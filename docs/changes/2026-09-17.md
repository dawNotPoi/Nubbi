# 变更记录 - 2026-09-17

> 本文件记录当日 UI 预览变更；各项验证结果见对应记录。

---

## [15:22] commit: `48ef72e8f4418922b89955c664bd2ef54cfab5a0` - style(client): 重构登录页为响应式工作台背景
**提交者**: ChatGPT
**变更文件**:
- `client/src/views/login/AuthShell.tsx`（修改）
- `client/src/views/login/AuthBrandPanel.tsx`（修改）

**Review 结果**: ⚠️ 代码结构审阅通过；Preview Deploy / 移动端真实视觉待 CI 与部署后验收

### 变更摘要
- 将认证页从左右硬分栏改为单一连续的暖色工作台背景，登录卡作为右侧悬浮任务区。
- 复用仓库既有 `/brand/auth-scene.webp` 与 Nubbi Mascot / Wordmark，不重新生成或替换 Logo。
- Desktop 保留品牌标题与轻量能力提示；Mobile 对同一背景做纵向裁切并隐藏功能提示，保留安全区与可滚动表单。
- 不修改 Better-Auth 登录、注册、邮箱验证和密码重置流程。

---

## [17:32] commit: `7e3efe97c30911a0017f59d3cb1b1696fa3947a0` - style(client): 接入确认的登录背景并保留移动端裁切
**提交者**: ChatGPT
**变更文件**:
- `client/public/brand/auth-workspace-20260917.avif`（新建）
- `client/public/brand/auth-workspace.md`（新建）
- `client/src/views/login/AuthShell.tsx`（修改）

**Review 结果**: ✅ 提交前静态审查、CI lint/build 与 8088 Preview Deploy 均通过；手机真机视觉仍需实际设备验收。

### 变更摘要
- 接入用户最后确认的背景原构图，保留书本上的文字、爱心与便签精灵贴纸，不再使用旧 auth-scene 作为认证背景。
- 使用独立 AVIF 静态资源，不将 base64 写入 TypeScript；保留 1672 × 941 尺寸，网页压缩参数与源 PNG 校验值写入资产说明。
- 去掉整页与右侧的漂白遮罩，避免原图的暖色桌面、光影和文字再次被淡化；登录卡 surface/95 单独保证表单可读性。
- Mobile 使用 38% 水平裁切，沿用可滚动单列、已有限宽与安全区布局；Desktop 仍为全页连续背景。
- 不修改 NubbiBrand、Logo、favicon、路由或 Better-Auth 业务逻辑。
- 按用户要求通过 PR 进入 preview，以触发 8088 的 Preview Deploy；不合并到 dev/out。

---

## [22:20] commit: `未提交` - style(auth): 收拢认证页品牌内容层级
**提交者**: ChatGPT
**变更文件**:
- `client/src/views/login/AuthBrandPanel.tsx`（修改）

**Review 结果**: ⚠️ 提交前静态审查通过；CI 与 8088 实际视觉待部署后验证。

### 变更摘要
- 保持已确认背景、Logo 与登录卡不变，仅重排左侧品牌叙事层。
- Logo、中文主标题、英文副标和功能提示统一到同一中心轴，减少背景上方元素散落感。
- 删除重复的“记录 · 整理 · 思考”文字行，将“记录 / 整理 / 协作”从三个绝对定位卡片改为紧凑的居中功能标签。
- Desktop 将品牌内容整体置于左侧可用区域中心并轻微上收；Mobile 继续隐藏功能标签，英文副标在最窄手机视口隐藏，避免拉长首屏。
- 不修改 Better-Auth、AuthShell 背景裁切、NubbiBrand、favicon 或任何认证业务语义。
