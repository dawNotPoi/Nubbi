# 认证模块讨论记录

## [2026-09-21 10:15] 第 1 轮：认证、会话与资源隔离重构

### 参与方

- 用户、Agent

### 已决策

| # | 决策 | 理由 |
| --- | --- | --- |
| 1 | 多用户、单一普通用户角色，各自使用自己的资源 | 用户最终澄清；不是全站仅一个账号 |
| 2 | 不新增管理员、角色分级或权限管理后台 | 产品不需要这些能力 |
| 3 | 保留注册、邮箱及第三方登录 | 多账号系统仍需完整认证入口 |
| 4 | 重构认证协调层，保留资源归属、会议与 API Key/MCP 边界 | 去掉重复实现不能扩大访问范围 |
| 5 | 先保留 Better Auth，升级/替换另行评估 | 已发现集成和状态缺陷，不需要先迁移所有身份数据 |
| 6 | 本地新分支 `refactor/auth-permissions`，不使用 codex 前缀 | 用户要求隔离本次重构并继续本地开发 |
| 7 | 保持已确认 UI 品牌与组件风格 | 本次是认证与状态重构，不是重新设计登录卡片 |

### 待决策

- [ ] 用户审阅 `auth-session-isolation-design.md` 与配套流程图，确认后进入实施计划。
- [ ] 执行阶段需要双账号/OAuth 等真实验证时，确认可用测试身份；不擅自创建或修改真实账号。
- [ ] 索引执行、数据库部署调整、依赖升级需在各自评估完成后另行确认。

### 关键上下文（给下一个 agent）

- 分支已从 `refactor/ui-unification-local` 创建；保留已提交的登录卡片和圆角统一成果。
- `website/` 是用户原有未跟踪目录，不属于此次修改。
- 核心代码：`client/src/utils/auth.ts`、`client/src/hooks/useAuth.ts`、`client/src/Route.tsx`、`client/src/AppProvider.tsx`、`server/app/middleware/authentication.ts`、`server/app/middleware/session.ts`。
- 当前 Better Auth 1.2.12 的全局成功回调覆盖问题已做隔离验证；现有 HTTP 日志显示重复会话查询。完整诊断依据与限制见设计文档。
- 当前服务端已拒绝 MCP Token 访问普通 Note 路由；旧 MCP PRD 的“可只读”描述落后于实现。
- 本轮只有设计和文档整理，没有业务实现、数据库写入或提交；不得把设计状态当作已完成。
- 项目禁止未经要求创建/修改测试文件；提交必须另获用户指令，不自动更新 changes。

### 讨论摘要

用户从登录卡顿问题提出全面重构，最终确认目标为多用户单角色系统的认证与资源隔离，而非管理员权限体系。诊断发现 JWT 接收回调、局部用户副本、路由等待及多入口恢复的组合问题。方案选择保留认证提供方，先统一客户端会话与凭证协调，再审查各服务端资源边界，最后进行独立性能优化。用户已确认产品范围；详细设计待审阅后进入实施计划。

## [2026-09-21 10:22] 第 2 轮：评估升级最新稳定版

### 参与方

- 用户、Agent

### 已决策

| # | 决策 | 理由 |
| --- | --- | --- |
| 1 | 用户允许把最新稳定版升级纳入考虑 | 不必继续围绕 1.2.12 构建新架构 |
| 2 | 升级与会话重构分开评估和验证 | 便于识别兼容问题及回退 |

### 待决策

- [ ] 审阅设计 2.1 与阶段 0；升级候选是当日 npm/GitHub 同步确认的 1.7.5，尚未安装。
- [ ] 隔离验证环境、API Key 数据兼容迁移及回退、新旧运行环境对齐方案经确认后实施；不直接启动新版连接共享远程库。

### 关键上下文（给下一个 agent）

- 此轮只读查询包元数据和官方文档，未修改 package.json、锁文件、运行代码或数据库。
- 新版 API Key 拆包，Schema/响应改为 referenceId 并增加 configId；必须同步鉴权、创建、列表、注销清理和防越权检查。
- 1.7.3 已撤回早期 1.7.0–1.7.2 的 account issuer 必填变更，不盲目迁移 issuer。
- 当前 Node 本地为 24.16.0，CI 混用 20/22，服务镜像 20；最终依赖图可能要求 >=22，需单独验证。
- 上一轮“升级以后再评估”的默认顺序被本轮建议的阶段 0 替代，但执行和数据库迁移尚未获确认。

### 讨论摘要

用户提出可以考虑最新稳定版。Agent 同时核实 npm 标签与官方发布，建议先完成 1.7.5 的独立兼容验证，再重构应用状态。现有 MongoDB driver 与 React 在新版声明支持范围内，但 API Key 数据、内部适配器调用和运行环境是明确迁移点；不能只修改版本号或承诺升级自动解决卡顿。

## [2026-09-21 10:35] 第 3 轮：批准执行与隔离验证

### 已决策

| # | 决策 | 理由 |
| --- | --- | --- |
| 1 | 用户批准执行，先完成阶段 0 独立升级 | 最新稳定版适配作为后续会话重构基线 |
| 2 | 本地原开发服务暂停；升级服务仅连接专用本地 MongoDB | 防止依赖热重载未经确认写入共享远程库 |
| 3 | 真实数据库迁移、提交与推送仍需单独授权 | 执行代码计划不等于批准真实数据变更 |

### 关键上下文

- 阶段 0 计划：`better-auth-upgrade-plan.md`；方案不替代后续 A–D 阶段。
- 专用容器 `nubbi-auth-upgrade-20260921`，仅绑定 `127.0.0.1:27028`，没有挂载真实数据。
- 用旧版 1.2.12 生成虚构双账号、会话、普通/MCP Key 作为兼容基线，不复制真实身份数据。
- 旧 MongoDB API Key 的 userId 实际为 ObjectId，迁移和清理不能只考虑字符串。

### 待决策

- [ ] 真实迁移的备份、停写窗口及执行授权。
- [ ] 真实 Google/GitHub OAuth 验收所需可用测试身份。

## [2026-09-21] 第 4 轮：阶段 0 隔离验收

### 已确认

- Better Auth 1.7.5 完整项目服务通过隔离 startup readiness；4 条旧 Key 显式迁移后为零 pending、零 conflict，非迁移字段与库内基线一致。
- 旧密码、旧 Session、旧 JWKS/JWT、普通 Key/MCP Key、限流、撤销、归属与 MCP 路由边界均通过隔离验证。
- 项目验证码桥接通过独立隔离进程验证，未发送邮件；合成账号注销会清理 canonical 与 legacy-layout Key，另一账号不受影响。
- 本轮只完成阶段 0，不替代阶段 A–D；未提交、未推送、未连接或复制真实数据库。

### 仍需授权或验收

- [ ] 真实数据库的备份、停写窗口、dry-run/apply 与生产启动。
- [ ] Google/GitHub 真实授权回调、真实 SMTP 传输和生产部署。
- [ ] 阶段 A/B 的唯一会话快照、代次隔离与 JWT single-flight，以及阶段 C/D 的资源审计和性能验收。

### 最终 review 与交接

- 阶段 0 的任务 review、整体 review 及最终修复定向复审均通过，无 Critical/Important。无效 canonical 归属不再回退旧字段，异常旧归属转换安全拒绝；最新容器构建与禁网纯 helper 验证通过。
- server 直接依赖 Zod 4 满足认证依赖，既有业务使用官方 zod/v3 兼容导出；未更改业务 Schema 语义。
- 只读索引门禁限定于 adapter 实际自动创建的 table-level indexes；当前声明全空，不要求额外建索引。选项等价比较的规范化延至首次新增这类索引前完成，否则可能安全地误拒启动。
- 原开发服务继续停止，现有数据库未迁移、未运行新版服务；本地代码未提交/推送。
- 下一步顺序待用户选择：建议隔离环境继续 A/B，真实迁移、OAuth 与 SMTP 在获授权后单独验收；不要将未回复的建议视为授权。

## [2026-09-21] 第 5 轮：会话与资源隔离实现收尾

### 已决策与落地

- 用户随后授权连续完成本地重构，并强调不要过度防御编程；范围固定为普通用户身份、自己的资源、现有分享与会议协作，无管理员/RBAC。✅ done
- 统一 coordinator、路由和请求层；清理多份身份与零调用兼容入口；缓存、上传、实时连接按账号生命周期处理。✅ done
- 资源审计未证实 owner 漏滤的模块保留现有实现，不为审计制造代码变更。✅ done
- 认证索引扩展为 12 个必要索引，启动仅检查，创建必须显式 apply；官方索引与项目索引共用语义比较。✅ done
- 用户指出周期过长；停止范围扩展与重复审查，只处理真实运行发现的问题和最终验收。

### 交接边界

运行证据与限制集中在 `auth-refactor-verification.md`；前述阶段 0 的“阶段 A–D 尚未执行”“当前不要求额外索引”是历史状态，已由本轮覆盖。代码未提交/推送，真实数据库仍未迁移，不能直接连接旧真实库启动新服务。第三方 OAuth/SMTP 与生产仍需独立验收。
